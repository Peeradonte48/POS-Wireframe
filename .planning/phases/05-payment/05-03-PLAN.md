---
phase: 05-payment
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-05

must_haves:
  truths:
    - "Staff can view an itemized bill with all non-voided items, modifier details, qty, and price per line"
    - "Coupon code + discount amount fields visible in totals; after Apply coupon line appears in correct position"
    - "VAT 7% calculated on post-discount subtotal; Grand Total is correct"
    - "Staff can select Cash, QR PromptPay, or Card — only one active at a time"
    - "Cash panel shows cash received input and change due; change due is red when underpayment"
    - "QR PromptPay panel shows 200×200 QR mock, total amount, and 'Scan to pay with PromptPay' label"
    - "Card panel shows total amount and 'Customer taps or swipes at card reader' copy"
    - "Confirm Payment transitions to receipt screen (same URL); receipt shows checkmark, table number, total, method, timestamp, annotated print note"
    - "After Confirm Payment, floor map table tile shows Cleaning status"
    - "Reprint Receipt fires Sonner toast 'Receipt sent to printer' with annotation below the button"
    - "Back to Floor Plan navigates to /table-map"
    - "Split bill placeholder button is disabled with 'ⓘ Seat-level split planned for v2' annotation"
  artifacts:
    - path: "src/app/(app)/payment/[tableId]/page.tsx"
      provides: "Full payment + receipt flow"
    - path: "src/components/payment/BillLineItem.tsx"
      provides: "Bill line rendering"
    - path: "src/components/payment/TotalsSection.tsx"
      provides: "Totals + coupon + split placeholder"
    - path: "src/components/payment/PaymentMethodSelector.tsx"
      provides: "Method segmented selector"
    - path: "src/components/payment/CashPanel.tsx"
      provides: "Cash received + change due"
    - path: "src/components/payment/QrPanel.tsx"
      provides: "Static QR mock"
    - path: "src/components/payment/CardPanel.tsx"
      provides: "Card instructional UI"
    - path: "src/components/payment/ReceiptScreen.tsx"
      provides: "Post-payment receipt confirmation"
    - path: "src/components/table-map/TableBottomSheet.tsx"
      provides: "Active Go to Payment entry point"
  key_links:
    - from: "TableBottomSheet (CheckRequested)"
      to: "/payment/[tableId]"
      via: "router.push"
    - from: "PaymentPage handleConfirmPayment"
      to: "table.store markCleaning + updateTable(orderStage: 'Billed')"
      via: "useTableStore.getState()"
    - from: "PaymentPage receipt view"
      to: "ReceiptScreen"
      via: "viewState === 'receipt' && receiptData"
---

<objective>
Browser verification checkpoint for all 5 PAY success criteria. Staff performs a full end-to-end payment flow in the browser and confirms each criterion.

Purpose: Phase gate — all PAY-01 through PAY-05 requirements must be confirmed working before Phase 5 can be marked complete and Phase 6 can begin.

Output: All 5 PAY success criteria checked off; Phase 5 marked complete.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-payment/05-CONTEXT.md
@.planning/phases/05-payment/05-01-SUMMARY.md
@.planning/phases/05-payment/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint — dev server ready check</name>
  <files></files>
  <action>
Confirm the development server is running and the payment route is reachable.

Run the type check and build one final time to confirm a clean baseline:
```bash
npx tsc --noEmit
npm run build
```

If build fails, fix errors before proceeding to the checkpoint.

The dev server should be running at http://localhost:3000. If it is not running, start it with `npm run dev` (in background).

Navigate to http://localhost:3000 in a browser to confirm the app loads.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>npm run build exits 0; dev server is accessible at localhost:3000</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 Payment flow:
- Itemized bill with modifier details, coupon/discount, VAT, total (PAY-01)
- Cash / QR PromptPay / Card payment method selection with per-method UI (PAY-02)
- Post-payment receipt confirmation screen with Cleaning status transition (PAY-03)
- Reprint Receipt Sonner toast (PAY-04)
- Split bill disabled placeholder with annotation (PAY-05)
- TableBottomSheet Go to Payment entry point activated
  </what-built>
  <how-to-verify>
**Setup — create a testable table state:**

1. Open http://localhost:3000, log in as Cashier or Waiter role, open a shift
2. On the floor map, open any table and seat it (set cover count)
3. Go to the order screen for that table, add 2–3 items with different modifiers, send the order to kitchen
4. Return to floor map, tap the table tile, tap "Request Check" in the bottom sheet — table should show CheckRequested status (purple/pink tile)

**PAY entry point:**

5. Tap the CheckRequested table tile — bottom sheet opens; tap "Go to Payment" — should navigate to /payment/[tableId]

**PAY-01 — Itemized bill:**

6. Verify: all items appear as individual lines with name on top, modifier summary text below (e.g. "Shoyu, Level 3, Chashu"), qty × price on right
7. Verify: voided items (if any) do NOT appear on the bill
8. Enter coupon code "TEST" and discount amount "50"; tap [Apply]
9. Verify: coupon line "Coupon TEST −฿50" appears in totals section
10. Verify: VAT 7% row appears; verify VAT = (subtotal − 50) × 7% (check manually)
11. Verify: Grand Total = discounted subtotal + VAT

**PAY-05 — Split bill placeholder:**

12. Verify: "Split Bill → v2" button is visible and disabled (grey, not clickable)
13. Verify: "ⓘ Seat-level split planned for v2" annotation visible beneath it

**PAY-02 — Payment methods:**

14. Select Cash: verify "Cash Received (฿)" input appears; enter an amount less than total — verify change due shows in red; enter amount >= total — verify change due shows in neutral color
15. Select QR PromptPay: verify 200×200 QR image appears, total amount above it, "Scan to pay with PromptPay" label below
16. Select Card: verify total amount displayed + "Customer taps or swipes at card reader" copy

**PAY-02 + PAY-03 — Confirm Payment:**

17. Select any payment method (ensure Cash has valid amount if Cash selected)
18. Tap "Confirm Payment — ฿XXX"; verify: page transitions to receipt screen (URL stays /payment/[tableId])
19. Verify receipt screen shows: ✔ "Payment Received" header, table number, total paid in ฿, payment method name, Thai-locale timestamp, "🖶 Invoice auto-printed [annotated]" annotation

**PAY-03 — Table Cleaning status:**

20. Tap "Back to Floor Plan"; verify: table tile on floor map shows Cleaning status (grey/broom icon)

**PAY-04 — Reprint:**

21. Navigate back to the receipt screen (you may need to re-enter payment flow OR test this before Back to Floor Plan)
22. Tap "Reprint Receipt"; verify: Sonner toast "Receipt sent to printer" appears at top of screen
23. Verify: "(annotated — no printer)" text is visible beneath the Reprint Receipt button

**Final check:**

24. Verify "Back to Floor Plan" button navigates to /table-map
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass. Describe any issues if not.</resume-signal>
</task>

</tasks>

<verification>
All 5 PAY success criteria from ROADMAP.md verified:
1. Itemized bill with line items, modifier details, discount, tax, total — PAY-01
2. Payment method selection (Cash/QR/Card) + Confirm → Cleaning — PAY-02, PAY-03
3. Receipt action state with annotated print — PAY-03
4. Reprint fires Sonner toast — PAY-04
5. Split bill v2 placeholder annotated — PAY-05
</verification>

<success_criteria>
- npm run build exits 0
- All 5 PAY criteria verified in browser by staff (human)
- Table transitions to Cleaning on floor map after payment confirmation
- Phase 5 can be marked complete in ROADMAP.md and STATE.md
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/05-payment/05-03-SUMMARY.md` with:
- Verification results for all 5 PAY criteria (pass/fail notes)
- Any issues found and resolved during verification
- Confirmation that Phase 5 is complete

Then update:
- `.planning/ROADMAP.md`: Phase 5 status → Complete; plans list updated to show 3 plans executed
- `.planning/STATE.md`: Phase → Phase 5 complete; current position → Ready to plan Phase 6
- `.planning/REQUIREMENTS.md`: PAY-01 through PAY-05 marked [x] complete
</output>
