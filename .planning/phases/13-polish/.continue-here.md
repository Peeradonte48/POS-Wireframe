---
phase: 13-polish
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-03-12T09:26:47.983Z
---

<current_state>
Phase 12 (Split Bill) just completed and verified. Phase 13 (Polish) is the next phase — not yet planned or started. Pausing before discussing/planning Phase 13.
</current_state>

<completed_work>

## Phase 12 — Split Bill ✓ COMPLETE

All 4 plans executed, verified, and committed:

- **12-01**: `bill.store.ts` created with full TypeScript split interfaces, floor+remainder equal split math, VAT-per-seat logic, 6 store actions. Amber OKLCH token added to `globals.css`.
- **12-02**: `SplitSheet.tsx` (5-state bottom sheet: mode-select → equal-config → equal-seats / per-seat-assign → per-seat-pay) + `SeatPaymentPanel.tsx` (inline per-seat payment reusing Cash/QR/Card panels).
- **12-03**: `TableTile.tsx` updated with amber "X/N paid" scissors badge in `top-2 right-2` slot, derived from `useBillStore`.
- **12-04**: Wire-up — Split Bill button activated in `TotalsSection`, `SplitSheet` mounted in payment page with auto-open on resume, `cancelSplit` added to `TableBottomSheet` before `markClean`.

### Bugs fixed during Phase 12 UAT:
1. `order.store` never cleared on table reopen → Send to Kitchen button always disabled
2. `assignItem` wiped ALL lineId assignments across seats (should only replace same seat)
3. Re-assign picker never rendered for assigned seat items (UI gap)
4. `assignItem` replaced existing seat qty instead of accumulating → last unit bounced back to unassigned

</completed_work>

<remaining_work>

- Phase 13: Polish — not yet started
  - POLISH-01: CVA variants, elevation tokens, brand color tokens for all new v1.2 screens
  - POLISH-02: Responsive layout at tablet (768px+) and mobile (375px) for all new modals/sheets
- Phase 14: Merge Bill
- Phase 15: Order Tracking

</remaining_work>

<decisions_made>

- `assignItem` accumulates qty into existing seat assignment (does not replace) — required for qty>1 items split across seats
- `removeAssignment(tableId, lineId, seatIndex)` added as targeted removal (vs `unassignItem` which removes all)
- Re-assign from seat section tracks `assigningFromSeatIndex` to enable clean move semantics
- `clearOrder(tableId)` added to `order.store` and called from `OpenTableModal` on table open

</decisions_made>

<blockers>
None.
</blockers>

<context>
v1.2 milestone is 1/4 phases done. Phase 13 is a pure polish pass — no new logic, just applying CVA variants, elevation shadows, and brand tokens to the new split bill screens. Should be straightforward since the token system is already established.

Phase 13 depends on Phase 12 existing (screens to polish), which is now stable.
</context>

<next_action>
Start with: /gsd:discuss-phase 13
(Gather design preferences for polish — CVA variants, elevation hierarchy, responsive breakpoints for SplitSheet, SeatPaymentPanel, and TableTile badge)
</next_action>
