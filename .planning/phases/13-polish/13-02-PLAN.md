---
phase: 13-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/payment/SplitSheet.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02

must_haves:
  truths:
    - "No raw green palette classes (bg-green-*, text-green-*) remain in SplitSheet.tsx"
    - "No raw <button> elements with bespoke border/hover classes remain in SplitSheet.tsx"
    - "No inline uppercase section label class clusters remain — all replaced with caps utility"
    - "Seat picker does not use flex-wrap — uses horizontal scroll row instead"
    - "TypeScript compiles cleanly after all replacements"
  artifacts:
    - path: "src/components/payment/SplitSheet.tsx"
      provides: "All POLISH-01/02 violations fixed — CVA variants, caps utility, responsive seat picker"
      contains: "Badge variant=\"settled\""
  key_links:
    - from: "src/components/payment/SplitSheet.tsx"
      to: "src/components/ui/badge.tsx"
      via: "<Badge variant='settled'> — consumes settled variant from Plan 01"
      pattern: "Badge.*variant.*settled"
    - from: "src/components/payment/SplitSheet.tsx"
      to: "src/components/ui/button.tsx"
      via: "<Button variant='option-card'> and <Button variant='ghost' size='sm'>"
      pattern: "Button.*variant.*option-card"
---

<objective>
Apply all POLISH-01 and POLISH-02 fixes to SplitSheet.tsx: replace the 8 documented violations (raw green badge classes, raw button elements, inline caps patterns) with CVA variants from Plan 01, and fix the seat picker responsive layout.

Purpose: Bring SplitSheet.tsx fully in line with the v1.1 quality bar — no raw palette classes, no bespoke button styles, no inline section label clusters, and no flex-wrap seat reflow at mobile breakpoints.
Output: Updated SplitSheet.tsx with all violations resolved. `npm run build && npm run lint` clean.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-polish/13-CONTEXT.md
@.planning/phases/13-polish/13-RESEARCH.md
@.planning/phases/13-polish/13-01-SUMMARY.md

<interfaces>
<!-- Key variants now available from Plan 01 -->

From src/components/ui/badge.tsx (after Plan 01):
```tsx
// Usage: <Badge variant="settled">Settled</Badge>
badgeVariants — settled: "bg-status-settled-bg text-status-settled border-status-settled/30"
```

From src/components/ui/button.tsx (after Plan 01):
```tsx
// Usage: <Button variant="option-card" onClick={...}>...</Button>
// Usage: <Button variant="ghost" size="sm" onClick={...}>← Back</Button>
buttonVariants — "option-card": "h-auto w-full flex-col items-start rounded-xl border-2 ..."
```

From src/app/globals.css:
```css
/* caps utility — use className="caps" to replace inline label clusters */
@utility caps {
  @apply text-xs font-semibold uppercase tracking-wide text-muted-foreground;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all POLISH-01 violations in SplitSheet.tsx</name>
  <files>src/components/payment/SplitSheet.tsx</files>
  <action>
Apply all 7 documented violation fixes to SplitSheet.tsx. Read the file first to confirm exact line content before editing, as line numbers may have shifted from the research snapshot.

**Fix 1 — Equal-seats Settled badge (search for `bg-green-100`):**
```tsx
// BEFORE
<Badge className="bg-green-100 text-green-700 border-green-200 dark:bg-green-900/30 dark:text-green-400">
  Settled
</Badge>

// AFTER
<Badge variant="settled">Settled</Badge>
```

**Fix 2 — Per-seat-pay Settled badge (second occurrence of same pattern):**
Same replacement as Fix 1. There are two settled badge sites — fix both.

**Fix 3 — Equal Split mode-select card (search for `setView('equal-config')`):**
```tsx
// BEFORE
<button
  onClick={() => setView('equal-config')}
  className="rounded-xl border-2 border-border hover:border-primary transition-colors p-4 text-left space-y-1 focus:outline-none focus-visible:ring-2"
>

// AFTER
<Button
  variant="option-card"
  onClick={() => setView('equal-config')}
>
```
Keep the inner content unchanged.

**Fix 4 — Per Seat mode-select card (search for `initPerSeatSplit`):**
```tsx
// BEFORE
<button
  onClick={() => { initPerSeatSplit(tableId, defaultGuestCount); setView('per-seat-assign') }}
  className="rounded-xl border-2 border-border hover:border-primary transition-colors p-4 text-left space-y-1 focus:outline-none focus-visible:ring-2"
>

// AFTER
<Button
  variant="option-card"
  onClick={() => { initPerSeatSplit(tableId, defaultGuestCount); setView('per-seat-assign') }}
>
```
Keep the inner content unchanged.

**Fix 5 — Back button in equal-config view (search for `setView('mode-select')`):**
```tsx
// BEFORE
<button onClick={() => setView('mode-select')} className="text-sm text-muted-foreground hover:text-foreground">
  ← Back
</button>

// AFTER
<Button variant="ghost" size="sm" onClick={() => setView('mode-select')}>← Back</Button>
```

**Fix 6 — Back button in per-seat-assign view (second back button, similar pattern):**
Same replacement pattern as Fix 5. Locate by context — it's in the per-seat-assign view header area. Replace with `<Button variant="ghost" size="sm" onClick={...}>← Back</Button>`.

**Fix 7 — Section labels with inline caps pattern (two occurrences):**
Search for `text-sm font-semibold text-muted-foreground uppercase tracking-wide` (or similar inline cluster):
```tsx
// BEFORE
<p className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">
  Unassigned ({unassignedItems.length})
</p>

// AFTER
<p className="caps">Unassigned ({unassignedItems.length})</p>
```
Fix all occurrences of this pattern (check for `text-sm.*uppercase` or `uppercase.*tracking-wide` patterns in the file).

After all fixes, ensure `Button` is imported from `@/components/ui/button` (it likely already is — verify the import statement is present and includes `Button`).

Verify no `bg-green-` or `text-green-` strings remain in the file:
```bash
grep -n "bg-green-\|text-green-" src/components/payment/SplitSheet.tsx
```
Expected: 0 results.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && grep -c "bg-green-\|text-green-" src/components/payment/SplitSheet.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
`grep -c "bg-green-\|text-green-" src/components/payment/SplitSheet.tsx` returns 0. No raw `<button className=` elements with bespoke border/hover classes remain. `npm run build` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix responsive seat picker layout (POLISH-02)</name>
  <files>src/components/payment/SplitSheet.tsx</files>
  <action>
Replace `flex flex-wrap` with the horizontal scroll row pattern in the seat picker(s). There are two seat picker locations in `renderPerSeatAssign` — the unassigned item assignment picker and the reassign picker. Both need the fix.

Search for `flex flex-wrap` in SplitSheet.tsx. For each occurrence in a seat picker context:

```tsx
// BEFORE
<div className="flex flex-wrap gap-2 justify-center">

// AFTER
<div className="flex overflow-x-auto gap-2 pb-1 snap-x">
```

The `snap-x` enables CSS scroll snapping for a snappier feel on touch. `pb-1` gives breathing room above the scrollbar on desktop. `overflow-x-auto` ensures no reflow regardless of seat count — single row horizontal scroll.

Do NOT change `overflow-y-auto` on the SplitSheet panel itself (`max-h-[85vh] overflow-y-auto`) — that is correct and must remain.

After the fix, confirm no `flex-wrap` remains in seat picker sections:
```bash
grep -n "flex-wrap" src/components/payment/SplitSheet.tsx
```
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && grep -n "flex-wrap" src/components/payment/SplitSheet.tsx && npm run build && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <done>
`grep -n "flex-wrap" src/components/payment/SplitSheet.tsx` returns 0 lines (or only non-seat-picker occurrences if any exist). `npm run build && npm run lint` exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All POLISH-01 and POLISH-02 violations fixed in SplitSheet.tsx:
- Settled badges now use `<Badge variant="settled">` with OKLCH green tokens
- Mode-select cards now use `<Button variant="option-card">`
- Back buttons now use `<Button variant="ghost" size="sm">`
- Section labels now use `caps` utility
- Seat picker uses horizontal scroll row instead of flex-wrap
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. Log in as any staff role and open a table with an active order
3. Navigate to Payment → tap "Split Bill"
4. **Mode select view:** Verify the Equal Split and Per Seat buttons look like cards (border, elevation, full-width) with hover:border-primary on mouseover
5. **Per Seat assign view:** Add 6+ seats, verify the seat picker scrolls horizontally in a single row (no wrapping to second line) at both desktop and 375px mobile viewport
6. **Equal split view:** Pay one seat, verify the "Settled" badge is a cool green — distinctly different from the amber split badge (check in both light and dark mode)
7. **Visual inspection:** Confirm section labels (e.g. "Unassigned") are small caps style (`caps` utility — smaller than before)
8. Verify no visual regressions on the rest of the payment flow
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe"
# No raw green palette violations
grep -c "bg-green-\|text-green-" src/components/payment/SplitSheet.tsx
# No flex-wrap in seat pickers
grep -c "flex-wrap" src/components/payment/SplitSheet.tsx
# Full build + lint
npm run build && npm run lint
```
</verification>

<success_criteria>
- `grep -c "bg-green-\|text-green-" src/components/payment/SplitSheet.tsx` returns 0
- `grep -c "flex-wrap" src/components/payment/SplitSheet.tsx` returns 0 (or zero in seat picker contexts)
- `npm run build && npm run lint` exits 0
- Human verifies settled badge is visually distinct green, option-card buttons look card-like, seat picker scrolls horizontally on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/13-polish/13-02-SUMMARY.md` following the summary template.
</output>
