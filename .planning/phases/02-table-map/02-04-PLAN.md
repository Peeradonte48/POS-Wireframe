---
phase: 02-table-map
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - FLOOR-01
  - FLOOR-02
  - FLOOR-03
  - FLOOR-04
  - FLOOR-05

must_haves:
  truths:
    - "All 5 FLOOR success criteria from ROADMAP.md are verifiable in the browser"
    - "npx tsc --noEmit && npx next build both complete without errors"
    - "The floor plan is the first screen visible after login and shift open"
  artifacts:
    - path: "src/components/table-map/TableTile.tsx"
      provides: "Rendered and verified — correct status visuals in browser"
    - path: "src/components/table-map/TableBottomSheet.tsx"
      provides: "Rendered and verified — all 5 status-specific bottom sheet variants"
    - path: "src/components/table-map/OpenTableModal.tsx"
      provides: "Rendered and verified — guest count input and table opens correctly"
  key_links:
    - from: "browser at /table-map"
      to: "useTableStore"
      via: "reactive tile updates after every state transition"
      pattern: "useTableStore"
---

<objective>
Final verification of Phase 2: run the automated type check + build, then walk through all 5 ROADMAP success criteria manually in the browser to confirm Phase 2 is complete and demo-ready.

Purpose: Catch any integration issues before marking the phase done. This is the sign-off gate.
Output: Confirmation that all 5 FLOOR success criteria pass.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-table-map/02-CONTEXT.md
@.planning/phases/02-table-map/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated checks</name>
  <files></files>
  <action>
Run both automated checks in sequence:

1. TypeScript strict mode check:
   ```bash
   npx tsc --noEmit
   ```
   Must exit with zero errors.

2. Next.js production build:
   ```bash
   npx next build
   ```
   Must complete without errors. Warnings about Next.js bundle size are acceptable.

If either fails, diagnose and fix before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit && echo "TSC OK" && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Both `npx tsc --noEmit` and `npx next build` complete without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 floor plan implementation:
- 12-table responsive grid with live status visuals (FLOOR-01)
- Tap-to-action: Open → bottom sheet → Open Table modal; Occupied/CheckRequested/Cleaning/Reserved → status-appropriate bottom sheet (FLOOR-02)
- Open Table modal with guest count input (FLOOR-03)
- Live dwell timer on Occupied tiles (FLOOR-04)
- Inline waiter assignment and table note in Occupied bottom sheet (FLOOR-05)
  </what-built>
  <how-to-verify>
Run `npx next dev` and open the browser. Complete this walkthrough:

**SC1 — Table status visuals (FLOOR-01):**
1. Navigate to `/table-map` (after login + shift open)
2. Confirm 12 tiles are visible in a responsive grid
3. Confirm T10 and T11 show blue border + CalendarClock icon + "Reserved" label
4. Confirm all other tiles show green border + CircleDot icon + "Open" label
5. Header shows "10 / 12 tables available"

**SC2 — Tap routing (FLOOR-02):**
6. Tap an Open tile → bottom sheet slides up with [Open Table] and [Mark Reserved]
7. Tap an Open tile → [Mark Reserved] → tile turns blue Reserved; count updates
8. Tap a Reserved tile → read-only sheet appears (no buttons)
9. Tap another Open tile → [Open Table] → Dialog modal opens

**SC3 — Open Table modal (FLOOR-03):**
10. In the modal, clear the input and click "Open Table" → button should remain disabled (guestCount < 1)
11. Enter `3` → click "Open Table" → modal closes, tile immediately shows red Occupied border, "3 guests", "0:00" timer

**SC4 — Dwell timer (FLOOR-04):**
12. Wait 3–5 seconds while looking at the Occupied tile — confirm timer increments live (0:03, 0:04...)

**SC5 — Waiter assignment and note (FLOOR-05):**
13. Tap the Occupied tile → bottom sheet opens
14. Type "Nok" in the waiter field → click elsewhere on the sheet → close and reopen the sheet → "Nok" persists
15. Type "Window seat" in the note field → blur → reopen → "Window seat" persists
16. Tap [Request Check] → tile turns amber "Check Requested", sheet closes
17. Tap CheckRequested tile → sheet shows disabled [Go to Payment] with "(Phase 5)" annotation
18. (Optional) To test Cleaning→Open: the CheckRequested→Cleaning transition is Phase 5 — test manually by temporarily calling `markCleaning` from the browser console if needed, then tap [Mark Clean] to confirm the tile resets to Open
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 2, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
- [ ] `npx tsc --noEmit` passes (zero errors)
- [ ] `npx next build` passes
- [ ] SC1: 12 tiles with correct status colors + icons + labels
- [ ] SC2: Tap routing works for Open → bottom sheet, Occupied → bottom sheet, Reserved → read-only
- [ ] SC3: Open Table modal validates guest count and transitions tile to Occupied
- [ ] SC4: Dwell timer increments live on Occupied tiles
- [ ] SC5: Waiter name and note persist in store (survive close + reopen of sheet)
</verification>

<success_criteria>
All 5 Phase 2 ROADMAP success criteria verified:
1. Staff can scan floor plan and identify each table's state via color AND icon — not color alone
2. Tapping a table performs the status-appropriate action
3. Staff can enter seat/cover count and see it reflected on the tile
4. Every occupied table displays a live dwell timer badge
5. Staff can assign a waiter and add a table-level note that persists
</success_criteria>

<output>
After completion, create `.planning/phases/02-table-map/02-04-SUMMARY.md` following the summary template.

Also update `.planning/ROADMAP.md`:
- Mark Phase 2 plans as complete (4/4)
- Update the phase progress row

Also update `.planning/STATE.md`:
- Set current position to "Phase 2 complete"
- Add any new decisions or patterns to Accumulated Context
</output>
