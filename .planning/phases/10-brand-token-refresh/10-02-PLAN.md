---
phase: 10-brand-token-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/components/table-map/TableTile.tsx
  - src/components/kds/KdsTicketCard.tsx
  - src/components/app-shell/AppSidebar.tsx
autonomous: true
requirements:
  - TOKEN-04

must_haves:
  truths:
    - "TableTile STATUS_CONFIG contains no raw Tailwind palette classes — borderClass and textClass use only token-based class names"
    - "KdsTicketCard timer color ternary uses token class names, not text-red-500 / text-amber-500 / text-green-500"
    - "KdsTicketCard BUMP button uses token class names, not bg-green-600 / hover:bg-green-500 / ring-green-400"
    - "AppSidebar shift-lock banner uses token class names for bg, border, and text — not bg-amber-50 / border-amber-200 / text-amber-700"
    - "grep for raw palette classes in all three files returns zero results"
    - "next build succeeds with no TypeScript errors after all replacements"
  artifacts:
    - path: "src/components/table-map/TableTile.tsx"
      provides: "STATUS_CONFIG with token-based borderClass and textClass strings"
      contains: "border-l-status-open"
    - path: "src/components/kds/KdsTicketCard.tsx"
      provides: "Timer color ternary and BUMP button using status tokens"
      contains: "text-status-occupied"
    - path: "src/components/app-shell/AppSidebar.tsx"
      provides: "Shift-lock banner using check-requested status tokens"
      contains: "bg-status-check-requested-bg"
  key_links:
    - from: "src/app/globals.css @theme inline"
      to: "src/components/table-map/TableTile.tsx"
      via: "Tailwind utility class generation from --color-status-* aliases"
      pattern: "border-l-status-open"
    - from: "src/app/globals.css @theme inline"
      to: "src/components/kds/KdsTicketCard.tsx"
      via: "Tailwind utility class generation from --color-status-* aliases"
      pattern: "text-status-occupied"
    - from: "src/app/globals.css @theme inline"
      to: "src/components/app-shell/AppSidebar.tsx"
      via: "Tailwind utility class generation from --color-status-check-requested* aliases"
      pattern: "bg-status-check-requested-bg"
---

<objective>
Replace every hardcoded Tailwind palette class in TableTile.tsx, KdsTicketCard.tsx, and AppSidebar.tsx with the semantic token-based classes defined in Plan 01.

Purpose: Complete the token hygiene pass — globals.css is the authoritative source, and these three files reference it by name rather than hard-coding colors.
Output: Three edited TSX files with zero raw palette classes remaining. All status rendering now driven by the semantic token layer.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-brand-token-refresh/10-CONTEXT.md
@.planning/phases/10-brand-token-refresh/10-RESEARCH.md
@.planning/phases/10-brand-token-refresh/10-01-SUMMARY.md

<interfaces>
<!-- Full hardcoded class audit from 10-RESEARCH.md — read before editing. -->

TableTile.tsx — STATUS_CONFIG record (lines ~22–27):
Current → Replacement:
  borderClass 'border-l-green-500'  → 'border-l-status-open'
  textClass   'text-green-600'      → 'text-status-open'
  borderClass 'border-l-red-500'    → 'border-l-status-occupied'
  textClass   'text-red-600'        → 'text-status-occupied'
  borderClass 'border-l-blue-500'   → 'border-l-status-reserved'
  textClass   'text-blue-600'       → 'text-status-reserved'
  borderClass 'border-l-amber-500'  → 'border-l-status-check-requested'
  textClass   'text-amber-600'      → 'text-status-check-requested'
  borderClass 'border-l-gray-400'   → 'border-l-status-cleaning'
  textClass   'text-gray-500'       → 'text-status-cleaning'

KdsTicketCard.tsx — timer color ternary (lines ~21–27):
Current:
  elapsedSeconds >= 900 ? 'text-red-500'
  : elapsedSeconds >= 600 ? 'text-amber-500'
  : 'text-green-500'
Replacement:
  elapsedSeconds >= 900 ? 'text-status-occupied'
  : elapsedSeconds >= 600 ? 'text-status-check-requested'
  : 'text-status-open'

KdsTicketCard.tsx — BUMP button classes (lines ~70–74):
Current:    'bg-green-600 hover:bg-green-500 active:scale-95 ring-2 ring-green-400'
Replacement: 'bg-status-open hover:bg-status-open/80 active:scale-95 ring-2 ring-status-open/60'

AppSidebar.tsx — shift-lock banner (line ~53):
Current:    '...bg-amber-50 border-b border-amber-200 text-amber-700...'
Replacement: '...bg-status-check-requested-bg border-b border-status-check-requested/30 text-status-check-requested...'

Note on Tailwind v4 directional border color:
Tailwind v4 auto-generates color utilities from --color-* tokens in @theme inline.
'border-l-status-open' should work. If it does NOT work (build warns or class produces no CSS),
fall back to the arbitrary value syntax: 'border-l-[color:var(--color-status-open)]'
Verify with next build after each file edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded classes in TableTile.tsx and KdsTicketCard.tsx</name>
  <files>
    src/components/table-map/TableTile.tsx
    src/components/kds/KdsTicketCard.tsx
  </files>
  <action>
**TableTile.tsx — STATUS_CONFIG:**

Read the file first. Locate the STATUS_CONFIG constant (the Record mapping TableStatus to {borderClass, textClass, label, Icon}).

Replace each hardcoded palette class string with its token equivalent:
- Open:           borderClass 'border-l-green-500' → 'border-l-status-open',  textClass 'text-green-600' → 'text-status-open'
- Occupied:       borderClass 'border-l-red-500'   → 'border-l-status-occupied',  textClass 'text-red-600' → 'text-status-occupied'
- Reserved:       borderClass 'border-l-blue-500'  → 'border-l-status-reserved',  textClass 'text-blue-600' → 'text-status-reserved'
- CheckRequested: borderClass 'border-l-amber-500' → 'border-l-status-check-requested',  textClass 'text-amber-600' → 'text-status-check-requested'
- Cleaning:       borderClass 'border-l-gray-400'  → 'border-l-status-cleaning',  textClass 'text-gray-500' → 'text-status-cleaning'

No other changes to TableTile.tsx.

**KdsTicketCard.tsx — timer color ternary and BUMP button:**

Read the file first. Locate:
1. The timerColorClass ternary (~lines 21–27) — replace:
   - 'text-red-500'   → 'text-status-occupied'
   - 'text-amber-500' → 'text-status-check-requested'
   - 'text-green-500' → 'text-status-open'

2. The BUMP button className (~lines 70–74) — replace:
   - 'bg-green-600'       → 'bg-status-open'
   - 'hover:bg-green-500' → 'hover:bg-status-open/80'
   - 'ring-green-400'     → 'ring-status-open/60'

No other changes to KdsTicketCard.tsx.

After both edits, run next build and confirm it succeeds. If 'border-l-status-open' produces a build warning about unknown class, switch to 'border-l-[color:var(--color-status-open)]' for all borderClass values.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && grep -n "bg-green-\|bg-red-\|bg-amber-\|bg-gray-\|text-green-\|text-red-\|text-amber-\|text-gray-\|border-l-green\|border-l-red\|border-l-blue\|border-l-amber\|border-l-gray" src/components/table-map/TableTile.tsx src/components/kds/KdsTicketCard.tsx; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TableTile.tsx STATUS_CONFIG has zero palette class strings — all 10 borderClass/textClass values use status token names
    - KdsTicketCard.tsx timer ternary uses text-status-* classes
    - KdsTicketCard.tsx BUMP button uses bg-status-open and ring-status-open/* classes
    - grep returns zero hits for raw palette classes in both files
    - next build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded classes in AppSidebar.tsx</name>
  <files>src/components/app-shell/AppSidebar.tsx</files>
  <action>
Read AppSidebar.tsx. Locate the shift-lock banner (line ~53) — the element that shows a warning when no shift is open. It currently uses:
  `bg-amber-50 border-b border-amber-200 text-amber-700`

Replace those three palette classes with token equivalents:
  `bg-amber-50`       → `bg-status-check-requested-bg`
  `border-amber-200`  → `border-status-check-requested/30`
  `text-amber-700`    → `text-status-check-requested`

The rest of the className string (flex, items-center, gap-2, px-3, py-2, border-b, text-xs) stays unchanged.

No other changes to AppSidebar.tsx.

After the edit, run the full grep verification across all three files and confirm next build succeeds.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && grep -n "bg-green-\|bg-red-\|bg-amber-\|bg-gray-\|text-green-\|text-red-\|text-amber-\|text-gray-\|border-l-green\|border-l-red\|border-l-blue\|border-l-amber\|border-l-gray\|border-amber" src/components/table-map/TableTile.tsx src/components/kds/KdsTicketCard.tsx src/components/app-shell/AppSidebar.tsx; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AppSidebar.tsx shift-lock banner uses bg-status-check-requested-bg, border-status-check-requested/30, and text-status-check-requested
    - grep across all three files returns zero hits for raw palette classes
    - next build succeeds — TOKEN-04 complete
  </done>
</task>

</tasks>

<verification>
Final phase gate — run after both tasks:
1. `grep -rn "bg-green-\|bg-red-\|bg-amber-\|bg-gray-\|text-green-\|text-red-\|text-amber-\|text-gray-\|border-l-green\|border-l-red\|border-l-blue\|border-l-amber\|border-l-gray" src/components/table-map/TableTile.tsx src/components/kds/KdsTicketCard.tsx src/components/app-shell/AppSidebar.tsx` — must return zero results
2. `npx next build` — succeeds cleanly
</verification>

<success_criteria>
- Zero raw Tailwind palette classes remain in TableTile.tsx, KdsTicketCard.tsx, or AppSidebar.tsx (TOKEN-04 complete)
- All three files reference only semantic token-based class names (status-open, status-occupied, etc.)
- next build succeeds — no TypeScript or Tailwind compilation errors
- Combined with Plan 01 output: all four TOKEN requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/10-brand-token-refresh/10-02-SUMMARY.md`
</output>
