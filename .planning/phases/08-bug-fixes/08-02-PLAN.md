---
phase: 08-bug-fixes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(kds)/kds/page.tsx
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "A Manager who navigates to /kds sees the KDS board — not a redirect to /table-map"
    - "Waiter and Cashier roles are still blocked from /kds"
  artifacts:
    - path: "src/app/(kds)/kds/page.tsx"
      provides: "KDS page with corrected role guard"
      contains: "role !== 'Kitchen' && role !== 'Manager'"
  key_links:
    - from: "src/app/(kds)/kds/page.tsx"
      to: "useSessionStore role"
      via: "useEffect guard condition"
      pattern: "role !== 'Kitchen' && role !== 'Manager'"
---

<objective>
Fix the over-restrictive role guard in the KDS page that blocks Manager access. The guard currently rejects any role that is not Kitchen. It must be expanded to allow Kitchen or Manager.

Purpose: ROLE_NAV_ACCESS already grants Manager access to kds. The page guard incorrectly overrides that permission.
Output: src/app/(kds)/kds/page.tsx — two targeted condition changes, no structural changes.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(kds)/kds/page.tsx
@src/lib/role-permissions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand KDS role guard to allow Manager</name>
  <files>src/app/(kds)/kds/page.tsx</files>
  <action>
Read src/app/(kds)/kds/page.tsx in full, then make exactly two condition changes:

**Change 1 — useEffect redirect condition (lines ~17-24):**

Current (buggy):
```typescript
} else if (role !== 'Kitchen') {
  router.replace('/table-map')
}
```

Fixed (allowlist both permitted roles):
```typescript
} else if (role !== 'Kitchen' && role !== 'Manager') {
  router.replace('/table-map')
}
```

**Change 2 — Early return render guard (line ~48):**

Current (buggy):
```typescript
if (role === null) return null
```

Fixed (prevent content flash for unpermitted roles):
```typescript
if (role === null || (role !== 'Kitchen' && role !== 'Manager')) return null
```

Do NOT change:
- The `if (role === null) { router.replace('/login') }` branch — this handles unauthenticated state and is correct
- ROLE_NAV_ACCESS in role-permissions.ts — already correct, no change needed
- The (kds)/layout.tsx — Manager should see the full-screen KDS layout without AppShell sidebar, which is already the correct behavior

Watch-out: Use an explicit allowlist (`!== 'Kitchen' && !== 'Manager'`), NOT a denylist or a null check. Changing to `role !== null` would incorrectly allow Waiter and Cashier into KDS.
  </action>
  <verify>
    Run: `npx tsc --noEmit` — no TypeScript errors.
    Manual (BUG-02): Log in as Manager, open shift, type `/kds` in the URL bar. Expected: KDS board renders with no redirect to /table-map.
    Manual (regression): Log in as Waiter, open shift, type `/kds` in the URL bar. Expected: Redirected to /table-map (Waiter must still be blocked).
  </verify>
  <done>
    Manager role sees the KDS board when navigating to /kds. Kitchen role continues to see KDS. Waiter and Cashier are redirected to /table-map. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
Manual smoke test (BUG-02):
1. Log in as Manager, open shift, navigate to /kds in URL bar → KDS renders
2. Log in as Waiter, open shift, navigate to /kds in URL bar → redirected to /table-map

Regression check:
3. Log in as Kitchen, navigate to /kds → KDS renders (unchanged behavior)
</verification>

<success_criteria>
A Manager role can navigate to /kds and see the kitchen display without being redirected. All other non-Kitchen/Manager roles are still correctly blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes/08-02-SUMMARY.md`
</output>
