---
phase: 07-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/app-shell/AppHeader.tsx
  - src/components/app-shell/AppShell.tsx
  - src/components/app-shell/AppSidebar.tsx
  - src/components/auth/PinNumpad.tsx
  - src/components/auth/RoleSelector.tsx
  - src/components/auth/ManagerPinModal.tsx
  - src/components/table-map/TableTile.tsx
  - src/components/table-map/TableBottomSheet.tsx
  - src/components/order/TicketLineItem.tsx
  - src/components/order/ModifierSheet.tsx
  - src/components/kds/KdsBoard.tsx
  - src/components/kds/KdsTicketCard.tsx
  - src/components/kds/KdsItemRow.tsx
  - src/components/kds/KdsRecallTray.tsx
  - src/components/payment/ReceiptScreen.tsx
  - src/app/(app)/order/page.tsx
  - src/app/(app)/payment/page.tsx
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02

must_haves:
  truths:
    - "No Lucide React imports remain in any .tsx file in the project"
    - "All icons render correctly using Solar iconStyle='Linear' or 'Bold' props"
    - "AppHeader shows 'A Ramen' wordmark with brand red 'A' and foreground 'Ramen'"
    - "ThemeToggle appears in AppHeader and functional"
    - "TypeScript build passes with no LucideIcon type reference errors"
  artifacts:
    - path: "src/components/app-shell/AppHeader.tsx"
      provides: "A Ramen wordmark + ThemeToggle + Solar LogOut icon"
      contains: "A Ramen"
    - path: "src/components/app-shell/AppSidebar.tsx"
      provides: "Nav icons migrated to Solar; LucideIcon type removed"
      contains: "solar-icon-set"
  key_links:
    - from: "AppSidebar.tsx NAV_ITEMS"
      to: "solar-icon-set named exports"
      via: "icon: ComponentType prop"
      pattern: "from 'solar-icon-set'"
    - from: "ThemeToggle"
      to: "AppHeader.tsx"
      via: "import in AppHeader"
      pattern: "ThemeToggle"
---

<objective>
Migrate ALL Lucide React icon imports to Solar Icon Set across every component file in the project. Add "A Ramen" wordmark and ThemeToggle to AppHeader. Remove lucide-react package after migration.

Purpose: This is the largest cross-cutting change in Phase 7. Doing icon migration as a dedicated plan prevents partial-migration build failures from contaminating other work.
Output: Zero Lucide imports in the codebase; all icons via solar-icon-set; A Ramen wordmark live.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-polish/07-RESEARCH.md
@.planning/phases/07-polish/07-01-SUMMARY.md

<interfaces>
<!-- Pre-researched Lucide-to-Solar mapping (from 07-RESEARCH.md) -->
<!--
IMPORTANT: Solar icon names must be verified at implementation time via:
  https://icones.js.org/collection/solar
If a name below doesn't compile, search icones.js.org for the closest Solar alternative.

| File | Lucide Import | Solar Equivalent | Style |
|------|--------------|------------------|-------|
| AppHeader.tsx | LogOut | LogOut | Linear |
| AppSidebar.tsx | LayoutGrid | WidgetFive | Linear |
| AppSidebar.tsx | ClipboardList | Notes | Linear |
| AppSidebar.tsx | Monitor | MonitorSmartphone | Linear |
| AppSidebar.tsx | CreditCard | CardTransferHorizontal | Linear |
| AppSidebar.tsx | BarChart3 | ChartSquare | Linear |
| AppSidebar.tsx | Lock | LockPassword | Linear |
| AppShell.tsx | PanelLeftClose/Open | SidebarMinimalistic | Linear |
| PinNumpad.tsx | Delete | Backspace | Linear |
| RoleSelector.tsx | Users | UsersGroupRounded | Linear |
| RoleSelector.tsx | DollarSign | DollarMinimalistic | Linear |
| RoleSelector.tsx | Shield | ShieldCheck | Linear |
| RoleSelector.tsx | ChefHat | Chef | Linear |
| ManagerPinModal.tsx | Shield | ShieldCheck | Bold |
| TableTile.tsx | CircleDot | RadioPoint | Linear |
| TableTile.tsx | Users | UsersGroupRounded | Linear |
| TableTile.tsx | CalendarClock | CalendarDate | Linear |
| TableTile.tsx | CreditCard | CardTransferHorizontal | Linear |
| TableTile.tsx | Sparkles | Star | Linear |
| TableTile.tsx | Clock | ClockCircle | Linear |
| TableBottomSheet.tsx | Clock | ClockCircle | Linear |
| TicketLineItem.tsx | Trash2 | TrashBinTrash | Linear |
| ModifierSheet.tsx | X | CloseCircle | Linear |
| ModifierSheet.tsx | Flame | Fire | Linear |
| order/page.tsx | ChevronLeft | AltArrowLeft | Linear |
| payment/page.tsx | ChevronLeft | AltArrowLeft | Linear |
| ReceiptScreen.tsx | CheckCircle | CheckCircle | Linear |
| ReceiptScreen.tsx | AlarmClock | ClockCircle | Linear |
| KdsBoard/KdsTicketCard/KdsItemRow/KdsRecallTray | (audit files directly for icons) | (map as found) | Linear/Bold |
-->

Solar usage pattern:
```typescript
import { TrashBinTrash, ClockCircle, AltArrowLeft } from 'solar-icon-set'
<TrashBinTrash size={15} iconStyle="Linear" />
<ClockCircle size={14} iconStyle="Bold" className="text-primary" />
```

AppSidebar LucideIcon type fix:
```typescript
// BEFORE: import type { LucideIcon } from 'lucide-react'
// AFTER: use inline type
type SolarIconComponent = React.ComponentType<{ size?: number; className?: string; iconStyle?: string }>
// Update NavItem interface icon field to: icon: SolarIconComponent
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AppShell group (AppHeader, AppShell, AppSidebar) + add wordmark + ThemeToggle</name>
  <files>src/components/app-shell/AppHeader.tsx, src/components/app-shell/AppShell.tsx, src/components/app-shell/AppSidebar.tsx</files>
  <action>
**AppSidebar.tsx:**
1. Remove `import type { LucideIcon } from 'lucide-react'`
2. Add local type alias: `type SolarIcon = React.ComponentType<{ size?: number; className?: string; iconStyle?: string }>`
3. Update the NavItem interface `icon` field to `icon: SolarIcon`
4. Replace all lucide-react icon imports with solar-icon-set equivalents:
   - LayoutGrid → WidgetFive
   - ClipboardList → Notes
   - Monitor → MonitorSmartphone
   - CreditCard → CardTransferHorizontal
   - BarChart3 → ChartSquare
   - Lock → LockPassword
5. Update NAV_ITEMS array to pass icon components (same as before, just Solar)
6. Update all icon JSX usages: add `iconStyle="Linear"` prop; remove className="..." size props if size prop handles sizing

**AppShell.tsx:**
1. Remove `import { PanelLeftClose, PanelLeftOpen } from 'lucide-react'`
2. Import `SidebarMinimalistic` from 'solar-icon-set'
3. Replace PanelLeftClose and PanelLeftOpen usages with `<SidebarMinimalistic size={18} iconStyle="Linear" />` for both (sidebar toggle works the same visually for open/close states via transform or conditional rendering)
4. Expand sidebar toggle button from h-7 w-7 to h-10 w-10 to meet 44px touch target (POLISH-02 fix)

**AppHeader.tsx:**
1. Remove `import { LogOut } from 'lucide-react'`
2. Import `{ LogOut2 }` (or `LogOut` — verify against Solar) from 'solar-icon-set'
3. Import `ThemeToggle` from '@/components/ui/theme-toggle'
4. Replace the plain branchName span with the "A Ramen" wordmark pattern:
```tsx
<div className="flex items-center gap-1.5 flex-1 min-w-0">
  <span className="font-bold text-lg tracking-tight text-primary">A</span>
  <span className="font-semibold text-base text-foreground truncate">
    {branchName ?? 'Ramen'}
  </span>
</div>
```
5. Add `<ThemeToggle />` before the logout button in the header right section
6. Update LogOut icon usage: `<LogOut size={16} iconStyle="Linear" />`

After changes: `npx tsc --noEmit` — fix any TypeScript errors before proceeding.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>AppSidebar has no LucideIcon import; AppHeader renders "A Ramen" wordmark with ThemeToggle; no TypeScript errors in these 3 files</done>
</task>

<task type="auto">
  <name>Task 2: Migrate auth, table-map, order, kds, and payment components + uninstall lucide-react</name>
  <files>src/components/auth/PinNumpad.tsx, src/components/auth/RoleSelector.tsx, src/components/auth/ManagerPinModal.tsx, src/components/table-map/TableTile.tsx, src/components/table-map/TableBottomSheet.tsx, src/components/order/TicketLineItem.tsx, src/components/order/ModifierSheet.tsx, src/components/kds/KdsBoard.tsx, src/components/kds/KdsTicketCard.tsx, src/components/kds/KdsItemRow.tsx, src/components/kds/KdsRecallTray.tsx, src/components/payment/ReceiptScreen.tsx, src/app/(app)/order/page.tsx, src/app/(app)/payment/page.tsx</files>
  <action>
For each file, read it first, identify all lucide-react imports, then replace with Solar equivalents per the mapping table in the interfaces block.

**Key substitutions to apply:**
- `Trash2` → `TrashBinTrash` (iconStyle="Linear", className="text-destructive" preserved)
- `Delete` (PinNumpad backspace key) → `Backspace` (iconStyle="Linear")
- `Flame` → `Fire` (iconStyle="Bold" for spice indicator — bold makes it more prominent)
- `X` → `CloseCircle` (iconStyle="Linear")
- `ChevronLeft` → `AltArrowLeft` (iconStyle="Linear")
- `CheckCircle` → `CheckCircle` (same name in Solar — verify it exists; fallback: `CheckCircleBold`)
- `AlarmClock` → `ClockCircle` (iconStyle="Linear")
- `Shield`, `ShieldCheck` → `ShieldCheck` (iconStyle="Bold" for ManagerPinModal, "Linear" elsewhere)
- `Users`, `UsersGroupRounded` → `UsersGroupRounded`
- `DollarSign` → `DollarMinimalistic`
- `ChefHat` → `Chef`
- `CircleDot`, `Clock`, `CalendarClock`, `CreditCard`, `Sparkles` in TableTile → per mapping table

**For KDS components:** Read the files, identify all Lucide imports (may include CheckSquare, ChevronRight, RefreshCw, Clock, etc.), and map to Solar equivalents:
- CheckSquare → `CheckSquare` or `CheckCircle` (verify Solar)
- ChevronRight → `AltArrowRight`
- RefreshCw → `Restart` or `ArrowCounterClockwise`
- Any remaining Clock → `ClockCircle`

**Icon usage pattern for ALL files:**
```typescript
import { IconName } from 'solar-icon-set'
<IconName size={N} iconStyle="Linear" />      // most cases
<IconName size={N} iconStyle="Bold" />         // status indicators, CTAs
```

After migrating all files and verifying tsc passes:
```bash
npm uninstall lucide-react
npx tsc --noEmit   # must still pass after removal
npx next build     # final build check
```

If any Solar icon name is not found (TypeScript error "has no exported member"), search for an alternative at https://icones.js.org/collection/solar and use the closest match. Leave a comment: `// Solar equivalent for Lucide {OriginalName}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>grep -r "lucide-react" src/ returns no results; tsc and next build both pass; all icon components render without TypeScript errors</done>
</task>

</tasks>

<verification>
Run `grep -r "lucide-react" src/` — must return empty.
Run `npx tsc --noEmit && npx next build` — both must pass.
Open browser at localhost:3000: verify icons render on Login, Table Map, and Order screens.
Toggle dark mode via ThemeToggle in AppHeader.
</verification>

<success_criteria>
- Zero lucide-react imports in src/ directory
- lucide-react removed from package.json
- solar-icon-set used for all icon rendering
- AppHeader shows "A Ramen" wordmark with brand red "A"
- ThemeToggle present in AppHeader
- npx tsc --noEmit passes clean
- npx next build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-02-SUMMARY.md`
</output>
