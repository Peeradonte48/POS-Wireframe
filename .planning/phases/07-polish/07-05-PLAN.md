---
phase: 07-polish
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified: []
autonomous: false
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04

must_haves:
  truths:
    - "npx next build completes with zero errors and zero TypeScript errors"
    - "Brand crimson color appears on all primary CTAs across all screens in light mode"
    - "Dark mode toggle switches all screens to dark theme with correct contrast"
    - "Solar icons render on every screen (no broken icon components)"
    - "A Ramen wordmark appears in AppHeader on all authenticated screens"
    - "Food photos appear in MenuPanel for ramen items"
    - "Role gating: Kitchen role cannot open tables or confirm payment"
    - "Role gating: non-Manager cannot toggle 86 or close shift"
    - "All 10 toasts fire for their respective actions"
    - "Touch targets: TicketLineItem buttons are tappable at 375px viewport"
    - "Loading skeletons appear for ~300ms on TableGrid and MenuPanel"
    - "Empty states render correctly on KDS board and manager tabs"
  artifacts:
    - path: "src/providers/ThemeProvider.tsx"
      provides: "ThemeProvider wrapping app root"
    - path: "src/lib/role-permissions.ts"
      provides: "ACTION_PERMISSIONS + canDoAction()"
  key_links:
    - from: "browser checkpoint"
      to: "all 4 POLISH criteria"
      via: "manual role-cycle test at 375px and 1024x768"
      pattern: "POLISH-01 POLISH-02 POLISH-03 POLISH-04"
---

<objective>
Final build verification and human browser checkpoint confirming all 4 POLISH requirements are satisfied across all screens and all roles.

Purpose: Phase 7 is the final phase of v1. This plan is the acceptance gate — nothing ships without a human confirming the demo-ready state with their own eyes.
Output: Confirmed demo-ready wireframe with all 34/34 v1 requirements complete.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-polish/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build verification</name>
  <files></files>
  <action>
Run the full validation suite in sequence:

```bash
npx tsc --noEmit
npx next build
```

If either command fails, fix ALL errors before proceeding to the checkpoint. Common issues to look for:
- Any remaining `lucide-react` import that was missed in Plan 02 migration
- Solar icon name that doesn't exist in solar-icon-set (will be a TypeScript error on the import)
- next/image hostname errors (should be resolved by next.config.ts change in Plan 01)
- Hydration warnings from missing suppressHydrationWarning (should be resolved in Plan 01)
- Broken canDoAction() calls from wrong ActionKey string

Fix any issues found, then re-run both commands until both pass clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>npx tsc --noEmit exits 0; npx next build exits 0 with "Route (app)" compilation table visible in output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 Hi-Fi & Brand complete:
    - Brand crimson primary color (oklch 0.52 0.22 27) on all CTAs
    - Dark mode toggle via ThemeToggle in AppHeader
    - Solar Icon Set replacing all Lucide icons
    - "A Ramen" wordmark in AppHeader
    - Inter font throughout
    - Unsplash food photos in MenuPanel
    - POLISH-01: ACTION_PERMISSIONS role gating on 10 in-screen actions
    - POLISH-02: 9 touch target violations fixed
    - POLISH-03: 10 Sonner toasts across all key actions
    - POLISH-04: Loading skeletons on TableGrid + MenuPanel, empty states on KDS + manager tabs
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:3000. Test in this sequence:

**POLISH-01 — Role Gating (test at 375px viewport in DevTools):**
1. Login as Waiter → Table Map → tap an open table → "Open Table" button should be ENABLED
2. Login as Kitchen → Table Map → tap a table → action buttons should be DISABLED (greyed out)
3. Login as Manager → Manager tab → EightySixTab → toggle should be ENABLED
4. Login as Waiter → Manager tab (shouldn't be accessible — sidebar shows locked)
5. Login as Cashier → TicketPanel → "Send to Kitchen" button should be DISABLED for Cashier
6. Login as Cashier → Payment screen → "Confirm Payment" should be ENABLED for Cashier

**POLISH-02 — Touch Targets (test at 375px width):**
7. Login as Waiter → Open any order → verify TicketPanel +/- quantity buttons feel tappable (expand hit area)
8. Verify AppSidebar nav items are tall enough for easy tapping
9. Payment screen → verify back button has sufficient tap area

**POLISH-03 — Toasts (trigger each):**
10. Open a table → toast "Table X opened — N guests" should appear
11. Send order to kitchen → toast "Order sent to kitchen" (existing)
12. Login as Manager → toggle any item in EightySixTab → toast "Item 86'd" or "Item available"
13. Manager → EodSummaryTab → Close Shift → toast "Shift closed"
14. Login as Waiter → try to void a sent item → manager PIN modal → cancel → toast "Void cancelled"
15. Cashier → confirm payment → toast "Payment confirmed"
16. Cashier → print receipt → toast "Receipt sent to printer" (existing)

**POLISH-04 — Loading + Empty States:**
17. Navigate to Table Map → for first ~300ms grey skeleton tiles should flash before real tiles appear
18. Open any order → MenuPanel skeleton rows should flash briefly then show food photos
19. Open KDS → if no tickets in a column, "No tickets" placeholder should be visible
20. Manager → EodSummaryTab with fresh store → "No orders this shift" message

**Brand + Visual:**
21. Verify primary buttons (Open Table, Send to Kitchen, Confirm Payment) appear in warm crimson not neutral black
22. Click ThemeToggle → all screens should switch to dark mode with correct contrast
23. Verify "A Ramen" wordmark is visible in AppHeader
24. Verify food photos appear for ramen menu items in MenuPanel
  </how-to-verify>
  <resume-signal>Type "approved" when all criteria pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 4 POLISH requirements verified by human browser testing across Waiter, Cashier, Manager, and Kitchen roles at both 375px and 1024×768 viewports.
</verification>

<success_criteria>
- npx next build exits 0
- All 20 verification steps pass in browser
- Brand crimson visible on all primary CTAs
- Dark mode functional
- Solar icons rendering on all screens
- A Ramen wordmark present
- Role gating working for all 10 audited actions
- 10 toasts firing for correct actions
- Loading skeletons and empty states working
- Phase 7 complete — all 34/34 v1 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-05-SUMMARY.md`

Also update:
- `.planning/STATE.md` — Phase 7 complete, status: complete
- `.planning/ROADMAP.md` — Phase 7 marked complete
- `.planning/REQUIREMENTS.md` — POLISH-01 through POLISH-04 marked [x]
</output>
