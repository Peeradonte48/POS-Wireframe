---
phase: 19-kds-differentiation-+-combo-flag
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-03-15T10:53:11.510Z
---

<current_state>
Phase 18 is fully complete and verified. Phase 19 is next — not yet discussed or planned.
Milestone v1.3 "Delivery & Takeaway Orders" is at 96% (7/8 phases complete, 21/21 plans).
</current_state>

<completed_work>

## Phase 18: Order Entry + Payment Pipeline — COMPLETE ✓

- **18-01**: queue.store `Sent→Ready` transition added; kds.store extended with `orderType`/`platform` optional fields; KdsBoard guard skips queue orders from auto-register; KdsTicketCard writes back to queue.store on InProgress bump
- **18-02**: order entry `onSend` for takeaway navigates to `/payment/TK-xxx` (was advancing queue directly); TakeawayCard derives live itemsSummary from order.store via useMemo
- **18-03**: payment page handles takeaway — `TK-001 · Jane Smith` header, Split/Merge hidden, `handleConfirmPayment` takeaway branch (Taking→Sent + addTicket('takeaway') + router.push('/table-map'), no receipt screen)
- **Bug fixes found in verification**: TicketPanel Send bypasses `send-to-kitchen` permission when custom `onSend` provided (Cashier can send takeaway); payment page Confirm bypasses `confirm-payment` for isTakeaway (Waiter can pay); AppSidebar badge counts active takeaway+delivery; Takeaway tab badge excludes Cancelled orders
- All verified end-to-end by human testing. VERIFICATION.md: human_needed → approved.

</completed_work>

<remaining_work>

## Phase 19: KDS Differentiation + Combo Flag — NOT STARTED

From ROADMAP.md: KDS badges/filter tabs showing order type (dine-in/takeaway/delivery), and combo "pack to go" flag. Phase 18 already laid the groundwork (kds.store.orderType field).

Steps needed:
1. `/gsd:discuss-phase 19` — capture design decisions
2. `/gsd:plan-phase 19` — research + plan
3. `/gsd:execute-phase 19` — execute

</remaining_work>

<decisions_made>

- Pay-at-ordering model: takeaway onSend → /payment/TK-xxx (not directly to kitchen); kitchen ticket created only after payment confirmed
- No receipt screen for takeaway — payment routes directly to /table-map
- Split/Merge hidden (not disabled) for takeaway payment
- Role permissions bypass: any role with new-takeaway permission can do the full takeaway flow (send + pay)
- itemsSummary derived live from order.store (not stored in queue.store)
- KDS write-back from callsite (KdsTicketCard.handleBump), not from within stores
- Takeaway tab badge counts Taking/Sent/Ready (excludes Collected + Cancelled)
- AppSidebar queue badge counts active takeaway + delivery (not just delivery Pending)

</decisions_made>

<blockers>
None — phase 18 fully complete and verified.
</blockers>

<context>
Phase 19 is the last phase in milestone v1.3. It adds visual differentiation to the KDS — order type badges and filter tabs using the `orderType` field already stored in KdsTicket from Phase 18. Possibly a "pack to go" combo flag. Phase 18 explicitly deferred these as out of scope, so Phase 19 should be purely additive with zero regression risk to the store layer.
</context>

<next_action>
Start with: `/gsd:discuss-phase 19` to capture design decisions before planning.
</next_action>
