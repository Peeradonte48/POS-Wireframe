---
phase: 15-order-tracking
task: 0
total_tasks: 7
status: in_progress
last_updated: 2026-03-13T01:36:08.271Z
---

<current_state>
Phase 15 planning is complete. All 3 PLAN.md files have been created and verified. Ready to execute. No tasks have been executed yet — this is a pre-execution pause after planning.
</current_state>

<completed_work>

- Research: 15-RESEARCH.md written — zero new deps, all data already in flight (table.orderStage, OrderRound.sentAt, kds.store.tickets)
- Validation: 15-VALIDATION.md written — build-only strategy, manual visual checks for all 3 requirements
- Plan 15-01: CSS token pairs (ordered/cooking/ready/escalated) + Badge CVA variants — Wave 1, autonomous
- Plan 15-02: order-tracking.ts utils + useSentTimer hook + TableTile color-coded badge — Wave 2, autonomous
- Plan 15-03: OrderTimeline component + TableBottomSheet tab wiring + human verify checkpoint — Wave 3, not autonomous
- Plan checker: VERIFICATION PASSED — all 3 requirements covered, Nyquist compliant
</completed_work>

<remaining_work>

- Task 0 (Wave 1): Execute Plan 15-01 — add 4 CSS token pairs to globals.css + 4 badge CVA variants in badge.tsx
- Task 0 (Wave 2): Execute Plan 15-02 — create src/lib/order-tracking.ts + src/components/table-map/useSentTimer.ts + update TableTile.tsx
- Task 0 (Wave 3): Execute Plan 15-03 — create OrderTimeline.tsx + update TableBottomSheet.tsx + human verify checkpoint
</remaining_work>

<decisions_made>

- Tab bar: two plain `<button>` elements with underline indicator (no new shadcn component import)
- Round header format: "Round N · HH:mm" using th-TH locale (consistent with dwell timer locale)
- ESCALATION_THRESHOLD_MS = 15 * 60 * 1000 in src/lib/order-tracking.ts (shared constant)
- Stage derivation: pure function — KDS ticket stage → display stage, "no ticket = Served" fallback
- Zustand selector pattern: select s.tickets (stable Record), derive in useMemo — avoids infinite loop
- RoundSection sub-component approach for useSentTimer (one hook per round, respects hooks rules)
- Per-round escalation: guard sentAt === null before Date.now() arithmetic to avoid false positives
</decisions_made>

<blockers>
None — clean pre-execution state.
</blockers>

<context>
Phase 15 is a pure display/derivation phase — no new store mutations, no new npm packages. Work splits cleanly across 3 waves:
1. Design tokens + CVA variants (foundation for all color rendering)
2. Pure utils + hook + tile badge (the "live" part of the feature)
3. Timeline component + sheet tab wiring (the "drill-down" part of the feature)

The most sensitive pattern is Zustand selector safety (same fix as Phase 14 getMergedSecondaries). The research file has full code examples for all patterns. The plan checker passed on first attempt with no revision needed.
</context>

<next_action>
Start with: `/gsd:execute-phase 15` — run all 3 waves sequentially. Wave 3 has a human-verify checkpoint that will pause for approval.
</next_action>
