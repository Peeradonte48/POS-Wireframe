---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Staff member can open the app, enter a PIN, and be routed to the correct interface for their role"
    - "Manager can authorize a restricted action via manager PIN modal without leaving the current screen"
    - "Staff must complete shift open before the main POS is accessible"
    - "Branch name and role badge are visible in the persistent navigation header on every screen"
    - "A second staff member logging in with a different role sees meaningfully different enabled/disabled actions"
  artifacts:
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Role selection -> PIN entry -> login"
    - path: "src/app/(app)/shift-open/page.tsx"
      provides: "Shift gate — must complete before table-map accessible"
    - path: "src/components/app-shell/AppHeader.tsx"
      provides: "Persistent header with branch name + role badge"
    - path: "src/components/auth/ManagerPinModal.tsx"
      provides: "In-app manager authorization overlay"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "/shift-open"
      via: "router.replace after successful login"
    - from: "src/app/(app)/shift-open/page.tsx"
      to: "/table-map"
      via: "router.replace after openShift called"
---

<objective>
Verify all five Phase 1 success criteria in a browser. This is a human-in-the-loop checkpoint — automated TypeScript checks are a necessary precondition but not sufficient. The interactive behaviors (shake animation, modal overlay, sidebar gating) require visual confirmation.

Purpose: Phase gate — confirms Phase 1 is complete before Phase 2 planning begins.
Output: All 5 AUTH requirements verified live. Phase 1 marked complete in STATE.md.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final type check and start dev server</name>
  <files></files>
  <action>
Run the automated checks first, then start the dev server for manual verification:

```bash
# From repo root
cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe"

# 1. Full type check — must be zero errors
npx tsc --noEmit

# 2. Lint check
npm run lint

# 3. Start dev server (Turbopack)
npm run dev
```

The dev server will be available at http://localhost:3000.

If `npx tsc --noEmit` or `npm run lint` return errors, fix them before proceeding to the checkpoint. Common issues to check:
- Missing `React` import in older files (App Router components use React 19 implicit imports — should be fine)
- `Label` component not installed — if shadcn Label is missing run `npx shadcn@latest add label`
- Any `any` type warnings from strict mode
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits with zero errors
    - `npm run lint` exits with zero errors or warnings
    - Dev server starts at http://localhost:3000
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 Foundation:
    - PIN login with role selection (Waiter / Cashier / Manager / Kitchen)
    - Shift Open gate screen (branch selection + Thai Baht opening cash)
    - AppShell: persistent header (branch + role badge + staff name) + collapsible sidebar
    - Role-filtered sidebar (Kitchen sees only KDS; Manager sees all)
    - Manager PIN override modal (reuses PinNumpad, shows action context)
  </what-built>
  <how-to-verify>
    Visit http://localhost:3000 and verify all 5 scenarios:

    **Scenario 1 — Waiter login flow (AUTH-01, AUTH-04, AUTH-05):**
    1. App redirects to /login — role selector shows 4 roles
    2. Select "Waiter" — PIN numpad appears (3x4 grid, no confirm button)
    3. Enter wrong PIN (e.g. 0000) — numpad shakes, input turns red, then auto-clears
    4. Enter correct Waiter PIN: 1234 — navigates to /shift-open
    5. On shift-open: sidebar is visible but all nav items show locked/greyed state
    6. Select "A Ramen — Thonglor", enter opening cash (e.g. 1500), click "Open Shift"
    7. Navigates directly to /table-map — NO intermediate confirmation screen
    8. Header shows: "A Ramen — Thonglor" | "Waiter" badge | "Somchai"
    9. Sidebar shows Table Map and Orders enabled; KDS, Payment, Manager are greyed out

    **Scenario 2 — Kitchen role (AUTH-02):**
    1. Log out (logout button in header)
    2. Log in as Kitchen (PIN: 5678)
    3. Open shift (any branch, any cash)
    4. Navigate to table-map — sidebar shows ONLY KDS enabled; all other items greyed out

    **Scenario 3 — Manager role sees all nav items (AUTH-02):**
    1. Log out, log in as Manager (PIN: 9999)
    2. Open shift
    3. All 5 sidebar items are enabled for Manager

    **Scenario 4 — Manager PIN override modal (AUTH-03):**
    1. While logged in as any non-manager role (e.g. Waiter), you cannot test this directly from the table-map placeholder page — instead, log in as any role and confirm the ManagerPinModal can be triggered
    2. (Developer note: the modal is a component, not yet wired to a button on a real page — Phase 3+ will wire it. At this stage: verify the component compiles and renders correctly by temporarily adding a test button to table-map/page.tsx if needed, OR defer this specific verification to Phase 3)
    3. If testing now: add a temporary "Test Manager Modal" button to /table-map placeholder that opens `<ManagerPinModal open={true} actionLabel="Authorize: Test Void" onAuthorize={() => alert('authorized')} onOpenChange={() => {}} />` — confirm modal appears as centered overlay with dark backdrop, action label shows, wrong PIN shakes, correct PIN (9999) triggers onAuthorize

    **Scenario 5 — Sidebar collapse (AUTH-05 / AppShell):**
    1. Click the collapse toggle in the sidebar
    2. Sidebar collapses to icon-only (no labels, ~64px wide)
    3. Click again — expands back to full width with labels
    4. Branch name + role badge + staff name remain visible in header throughout
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 scenarios pass.
    If issues found, describe what failed and Claude will fix before Phase 2 planning.
  </resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 1 roadmap success criteria verified in browser:
1. PIN login with role routing works for all 4 roles
2. Manager can authorize via modal without leaving current screen
3. Shift open is required before POS is accessible
4. Branch name and role badge visible in header on every (app)/ screen
5. Kitchen vs Manager show meaningfully different enabled actions in sidebar
</verification>

<success_criteria>
- All automated checks (tsc, lint) pass
- All 5 browser scenarios pass per human verification
- Phase 1 is demo-ready: role selector -> PIN login -> shift open -> app shell
</success_criteria>

<output>
After checkpoint approval:
1. Create `.planning/phases/01-foundation/01-05-SUMMARY.md`
2. Update `.planning/STATE.md`:
   - Phase 1 status: Complete
   - Current position: Phase 2 ready
   - Plans created: 5, Plans complete: 5
3. Update `.planning/ROADMAP.md` Phase 1 progress row
</output>
