---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/shift-open/page.tsx
  - src/app/(app)/table-map/page.tsx
  - src/stores/session.store.ts
  - src/lib/role-permissions.ts
  - src/lib/mock-data/staff.ts
  - src/lib/mock-data/branches.ts
  - tsconfig.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "npx tsc --noEmit passes with zero errors on the scaffolded project"
    - "The Zustand session store exports Role type, useSessionStore hook, and all four actions (login, openShift, logout, plus role/branch/staffName/shiftOpen state)"
    - "ROLE_NAV_ACCESS map covers all four roles (Waiter, Cashier, Manager, Kitchen) with correct nav slug sets"
    - "verifyPin(role, pin) returns the matching StaffMember or null against the four mock staff fixtures"
    - "Route group directories exist: (auth)/login and (app)/shift-open and (app)/table-map"
  artifacts:
    - path: "src/stores/session.store.ts"
      provides: "Typed Zustand session store — Role union type, SessionState interface, useSessionStore hook"
      exports: ["Role", "useSessionStore"]
    - path: "src/lib/role-permissions.ts"
      provides: "ROLE_NAV_ACCESS map from Role to NavSlug array"
      exports: ["NavSlug", "ROLE_NAV_ACCESS"]
    - path: "src/lib/mock-data/staff.ts"
      provides: "Mock staff fixtures and verifyPin helper"
      exports: ["MOCK_STAFF", "verifyPin", "StaffMember"]
    - path: "src/lib/mock-data/branches.ts"
      provides: "Mock branch list for shift-open dropdown"
      exports: ["BRANCHES", "Branch"]
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first config with @theme block including animate-shake keyframes"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "src/lib/mock-data/staff.ts"
      to: "src/stores/session.store.ts"
      via: "imports Role type"
      pattern: "import.*Role.*session.store"
    - from: "src/lib/role-permissions.ts"
      to: "src/stores/session.store.ts"
      via: "imports Role type"
      pattern: "import.*Role.*session.store"
---

<objective>
Scaffold the Next.js 15 project from scratch, configure Tailwind v4, initialize shadcn/ui, establish the route group structure, and write all Wave 0 type contracts (Zustand session store, role-permissions map, mock data) that every subsequent plan depends on.

Purpose: Every other plan in Phase 1 imports from files created here. Get the TypeScript interfaces right once — all downstream plans follow them.
Output: A compilable Next.js 15 app with correct route structure, Zustand store with exported Role type, and typed mock fixtures.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and configure Tailwind v4</name>
  <files>
    package.json,
    tsconfig.json,
    src/app/globals.css,
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/(auth)/layout.tsx,
    src/app/(auth)/login/page.tsx,
    src/app/(app)/layout.tsx,
    src/app/(app)/shift-open/page.tsx,
    src/app/(app)/table-map/page.tsx
  </files>
  <action>
Run the following commands in order from the repo root (`/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe`):

```bash
# 1. Scaffold — create-next-app writes into the current directory
npx create-next-app@latest . --yes --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack

# 2. Install Zustand
npm install zustand

# 3. Initialize shadcn/ui (accept all defaults — it detects Next.js + Tailwind v4)
npx shadcn@latest init --yes

# 4. Add all shadcn components needed in Phase 1
npx shadcn@latest add button input select dialog badge skeleton

# 5. Install Lucide React (shadcn may have added it; run idempotently)
npm install lucide-react
```

After the scaffold completes:

**Verify tsconfig.json has `"strict": true`.** If `create-next-app` did not set it, open `tsconfig.json` and add `"strict": true` under `compilerOptions`. This is mandatory.

**Update `src/app/globals.css`** — replace the entire file contents with:

```css
@import "tailwindcss";

@theme {
  /* Brand colors */
  --color-brand-primary: oklch(0.55 0.18 262);

  /* Shake animation for wrong PIN feedback */
  --animate-shake: shake 0.5s ease-out;

  @keyframes shake {
    10%, 90% { transform: translate3d(-1px, 0, 0); }
    20%, 80% { transform: translate3d(2px, 0, 0); }
    30%, 50%, 70% { transform: translate3d(-4px, 0, 0); }
    40%, 60% { transform: translate3d(4px, 0, 0); }
  }
}
```

Do NOT create a `tailwind.config.js` file. Tailwind v4 uses CSS-first config only.

**Create route group directory structure** and write placeholder files:

`src/app/page.tsx` — root redirect to login:
```typescript
import { redirect } from 'next/navigation'
export default function RootPage() {
  redirect('/login')
}
```

`src/app/(auth)/layout.tsx` — minimal centered layout, no AppShell:
```typescript
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background">
      {children}
    </div>
  )
}
```

`src/app/(auth)/login/page.tsx` — placeholder (Plan 02 will implement):
```typescript
export default function LoginPage() {
  return <div className="text-muted-foreground">Login — Plan 02</div>
}
```

`src/app/(app)/layout.tsx` — placeholder auth guard (Plan 03 will implement fully):
```typescript
'use client'
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
```

`src/app/(app)/shift-open/page.tsx` — placeholder (Plan 04 will implement):
```typescript
export default function ShiftOpenPage() {
  return <div className="p-8 text-muted-foreground">Shift Open — Plan 04</div>
}
```

`src/app/(app)/table-map/page.tsx` — placeholder for Phase 2:
```typescript
export default function TableMapPage() {
  return <div className="p-8 text-muted-foreground">Table Map — Phase 2</div>
}
```
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits with no TypeScript errors
    - `src/app/globals.css` contains `@import "tailwindcss"` and `@theme` block with `animate-shake` definition
    - No `tailwind.config.js` file exists in the project root
    - Route directories `(auth)/login` and `(app)/shift-open` and `(app)/table-map` all exist with placeholder pages
    - `tsconfig.json` has `"strict": true`
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Zustand session store, role-permissions map, and mock data fixtures</name>
  <files>
    src/stores/session.store.ts,
    src/lib/role-permissions.ts,
    src/lib/mock-data/staff.ts,
    src/lib/mock-data/branches.ts
  </files>
  <action>
Create the following four files exactly as specified. These are the Wave 0 type contracts — all other Phase 1 plans import from them. Do not deviate from the exported interface shapes.

**`src/stores/session.store.ts`:**
```typescript
import { create } from 'zustand'

export type Role = 'Waiter' | 'Cashier' | 'Manager' | 'Kitchen'

interface SessionState {
  // Auth
  role: Role | null
  staffName: string | null
  staffId: string | null
  // Branch + shift
  branch: string | null
  branchName: string | null
  openingCash: number | null
  shiftOpen: boolean
  // Actions
  login: (role: Role, staffName: string, staffId: string) => void
  openShift: (branch: string, branchName: string, openingCash: number) => void
  logout: () => void
}

export const useSessionStore = create<SessionState>((set) => ({
  role: null,
  staffName: null,
  staffId: null,
  branch: null,
  branchName: null,
  openingCash: null,
  shiftOpen: false,

  login: (role, staffName, staffId) => set({ role, staffName, staffId }),

  openShift: (branch, branchName, openingCash) =>
    set({ branch, branchName, openingCash, shiftOpen: true }),

  logout: () =>
    set({
      role: null,
      staffName: null,
      staffId: null,
      branch: null,
      branchName: null,
      openingCash: null,
      shiftOpen: false,
    }),
}))
```

Note: No `persist` middleware. Intentional — each page load starts fresh at login. Do not add persist.

**`src/lib/role-permissions.ts`:**
```typescript
import type { Role } from '@/stores/session.store'

export type NavSlug = 'table-map' | 'orders' | 'kds' | 'payment' | 'manager'

export const ROLE_NAV_ACCESS: Record<Role, NavSlug[]> = {
  Waiter:  ['table-map', 'orders'],
  Cashier: ['table-map', 'orders', 'payment'],
  Manager: ['table-map', 'orders', 'kds', 'payment', 'manager'],
  Kitchen: ['kds'],
}

export function canAccess(role: Role, slug: NavSlug): boolean {
  return ROLE_NAV_ACCESS[role].includes(slug)
}
```

**`src/lib/mock-data/staff.ts`:**
```typescript
import type { Role } from '@/stores/session.store'

export interface StaffMember {
  id: string
  name: string
  role: Role
  pin: string // 4-digit string
}

export const MOCK_STAFF: StaffMember[] = [
  { id: 's1', name: 'Somchai',  role: 'Waiter',   pin: '1234' },
  { id: 's2', name: 'Nida',     role: 'Cashier',  pin: '2345' },
  { id: 's3', name: 'Prayuth',  role: 'Manager',  pin: '9999' },
  { id: 's4', name: 'Malee',    role: 'Kitchen',  pin: '5678' },
]

export function verifyPin(role: Role, pin: string): StaffMember | null {
  return MOCK_STAFF.find((s) => s.role === role && s.pin === pin) ?? null
}
```

**`src/lib/mock-data/branches.ts`:**
```typescript
export interface Branch {
  id: string
  name: string
}

export const BRANCHES: Branch[] = [
  { id: 'b1', name: 'A Ramen — Thonglor' },
  { id: 'b2', name: 'A Ramen — Ekkamai' },
  { id: 'b3', name: 'A Ramen — Asok' },
]
```
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits with no errors
    - `src/stores/session.store.ts` exports `Role` union type and `useSessionStore` hook with typed `SessionState` interface
    - `src/lib/role-permissions.ts` exports `NavSlug`, `ROLE_NAV_ACCESS`, and `canAccess` — `ROLE_NAV_ACCESS['Kitchen']` contains only `'kds'`
    - `src/lib/mock-data/staff.ts` exports `MOCK_STAFF` (4 members, one per role) and `verifyPin` function
    - `src/lib/mock-data/branches.ts` exports `BRANCHES` with 3 A Ramen branch locations
  </done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` from the repo root — must exit cleanly with zero errors.
Confirm no `tailwind.config.js` exists.
Confirm `src/stores/session.store.ts` exports `Role` type with exactly four values: Waiter, Cashier, Manager, Kitchen.
Confirm `ROLE_NAV_ACCESS['Kitchen']` is `['kds']` — no other slugs.
</verification>

<success_criteria>
- Next.js 15 project builds without TypeScript errors
- Tailwind v4 CSS-first config with animate-shake in globals.css
- Route group structure: (auth)/ and (app)/ with placeholder pages
- Zustand session store typed with Role, SessionState interface, and four actions
- Role-permissions map covering all four roles
- Mock staff fixtures (4 staff members, one per role) and verifyPin helper
- Mock branch list (3 A Ramen locations)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
