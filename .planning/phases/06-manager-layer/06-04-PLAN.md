---
phase: 06-manager-layer
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: []
autonomous: false
requirements: [SHIFT-01, SHIFT-02, SHIFT-03, SHIFT-04]

must_haves:
  truths:
    - "Manager can close shift and see EOD summary with realistic financial numbers"
    - "Manager can view sales snapshot with revenue, covers, top items as numbers"
    - "Manager can toggle 86'd items and see immediate effect on order screen"
    - "Manager can view all open tickets and staff list"
    - "Non-Manager roles do not see manager nav item in sidebar"
    - "npx next build completes with zero errors"
  artifacts:
    - path: "src/app/(app)/manager/page.tsx"
      provides: "Complete 4-tab manager dashboard"
    - path: "src/stores/manager.store.ts"
      provides: "86'd state + shiftClosed persisted"
  key_links:
    - from: "AppSidebar"
      to: "/manager route"
      via: "nav item hidden for non-Manager, active for Manager"
      pattern: "slug === 'manager'"
---

<objective>
Final build verification and browser checkpoint for all 4 SHIFT requirements.

Purpose: Validate the complete Phase 6 implementation in-browser before marking the phase complete.
Output: Verified manager layer with all 4 success criteria confirmed.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-manager-layer/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <files>src/lib/mock-data/tables.ts</files>
  <action>
Run full TypeScript check and Next.js production build to confirm zero errors:

```bash
cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe"
npx tsc --noEmit && npx next build
```

Fix any errors found before proceeding to the checkpoint. Common issues to watch for:
- TableRecord missing new fields in INITIAL_TABLES mock data — add `paidAmount: null, paymentMethod: null, discountApplied: null` to each table object in `src/lib/mock-data/tables.ts`
- Import path errors (ensure @/stores/manager.store resolves)
- Type mismatch on paymentMethod field if `'Cash' | 'QR PromptPay' | 'Card' | null` in TableRecord doesn't align with PaymentPage's local `PaymentMethod` type — make them identical or use a shared type alias
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>npx tsc --noEmit exits 0. npx next build completes successfully with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification — all 4 SHIFT criteria</name>
  <files></files>
  <action>Human verifies Phase 6 manager layer in the browser. All 5 verification groups below must pass.</action>
  <verify>
    <automated>MISSING — manual browser verification required; run `npm run dev` first</automated>
  </verify>
  <done>All 5 verification groups confirmed. Type "approved" to complete.</done>
  <what-built>
Complete Phase 6 Manager Layer:
- /manager route with 4-tab dashboard (EOD Summary, Sales Snapshot, 86'd Items, Open Tickets)
- EOD Summary: financial totals, payment breakdown, cash reconciliation with reactive Over/Short variance, Close Shift dialog + read-only banner
- Sales Snapshot: revenue, VAT, covers, top items as numbers (no charts)
- 86'd Items: full menu item list by category with checkbox toggles
- Open Tickets: occupied table rows (tappable) + staff list with role badges
- MenuPanel: 86'd items appear greyed with badge and are not tappable
- AppSidebar: manager nav item hidden for non-Manager roles
  </what-built>
  <how-to-verify>
Run `npm run dev` then open http://localhost:3000

**1. Manager nav visibility (all SHIFT requirements)**
- Login as Waiter (PIN 1234), open shift → verify NO "Manager" item in sidebar
- Login as Manager (PIN 9999), open shift → verify "Manager" IS visible in sidebar

**2. EOD Summary tab (SHIFT-01)**
- Place at least one order: login as Waiter, seat a table, add a ramen bowl, send order
- As Cashier, go to payment, set method to Cash, confirm payment
- Login as Manager → navigate to /manager → EOD Summary tab should be default
- Verify: Sales Summary card shows non-zero Revenue, VAT, Net Sales
- Verify: Payment Breakdown shows Cash row with non-zero amount, QR and Card as ฿0
- Verify: Adjustments shows Discount Total (฿0 if no coupon used)
- Verify: Cash Reconciliation — type a number in the closing cash field → Over/Short variance updates immediately (green for over, red for short)
- Tap "Close Shift" → confirm dialog appears → confirm → "Shift Closed" banner appears, all inputs disabled, Logout button visible
- Tap Logout → redirected to login screen

**3. Sales Snapshot tab (SHIFT-02)**
- Login as Manager → /manager → Sales Snapshot tab
- Verify: 4 stat cards (Net Sales, VAT, Gross Revenue, Covers) with correct numbers
- Verify: Top Items list shows item names with "X sold" — no chart elements

**4. 86'd Items tab (SHIFT-03)**
- Login as Manager → /manager → 86'd Items tab
- Verify: all menu items displayed, grouped by category (Ramen, Sides, Drinks)
- Toggle one item to 86'd (checkbox checked, badge appears)
- Navigate to /table-map → open an order → go to order screen
- Verify: the 86'd item appears greyed out with "86'd" badge, clicking does nothing
- Navigate back to /manager → 86'd Items → item is still checked (persisted across navigation)
- Uncheck the item → navigate back to order screen → item is tappable again

**5. Open Tickets tab (SHIFT-04)**
- Ensure at least one table is occupied (from step 2)
- Login as Manager → /manager → Open Tickets tab
- Verify: occupied table appears as a row with table label, waiter name, cover count, order stage badge, estimated total, elapsed time
- Tap the table row → navigates to /order/[tableId]
- Back to /manager → Staff List section shows all 4 mock staff (Somchai/Waiter, Nida/Cashier, Prayuth/Manager, Malee/Kitchen) with role badges
  </how-to-verify>
  <resume-signal>Type "approved" when all 5 verification groups pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 6 complete when:
1. npx next build passes with zero errors
2. All 4 SHIFT success criteria verified in browser:
   - SHIFT-01: EOD summary with close shift flow
   - SHIFT-02: Sales snapshot numbers dashboard
   - SHIFT-03: 86'd toggle with order screen integration
   - SHIFT-04: Open tickets list + staff list
3. Manager nav hidden for non-Manager roles confirmed
</verification>

<success_criteria>
- Build passes (tsc --noEmit + next build)
- All 4 SHIFT criteria verified by human reviewer in browser
- Phase 6 marked complete in ROADMAP.md and STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-manager-layer/06-04-SUMMARY.md`
</output>
