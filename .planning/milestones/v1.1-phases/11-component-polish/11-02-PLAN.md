---
phase: 11-component-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/order/TicketPanel.tsx
  - src/components/payment/TotalsSection.tsx
  - src/components/manager/SalesSnapshotTab.tsx
  - src/components/manager/EodSummaryTab.tsx
  - src/components/manager/EightySixTab.tsx
  - src/components/manager/OpenTicketsTab.tsx
  - src/components/kds/KdsBoard.tsx
  - src/components/kds/KdsRecallTray.tsx
  - src/app/(app)/payment/[tableId]/page.tsx
autonomous: true
requirements:
  - COMP-04
  - COMP-05

must_haves:
  truths:
    - "The running total in the ticket footer reads as a large crimson number — visually dominant over surrounding text"
    - "The grand total in the payment summary reads as a large crimson number — the focal payment amount"
    - "All section labels (Manager tabs, KDS columns, ticket panel sections) look visually identical — same size, weight, tracking"
    - "No inline occurrence of the caps pattern remains — all replaced by className='caps'"
  artifacts:
    - path: "src/app/globals.css"
      provides: "@utility caps block defining the shared section label style"
      contains: "@utility caps"
    - path: "src/components/order/TicketPanel.tsx"
      provides: "hero total text with text-2xl font-black text-primary; caps label on 'Total' line"
      contains: "text-2xl font-black text-primary"
    - path: "src/components/payment/TotalsSection.tsx"
      provides: "hero grand total text with text-2xl font-black text-primary"
      contains: "text-2xl font-black text-primary"
  key_links:
    - from: "globals.css @utility caps"
      to: "all 13 section label occurrences"
      via: "className=\"caps\" replacement"
      pattern: "className=\"caps\""
    - from: "TicketPanel footer total paragraph"
      to: "text-2xl font-black text-primary"
      via: "direct className update on line ~145"
      pattern: "text-2xl font-black text-primary"
---

<objective>
Apply hero price readouts and standardize all section labels via the caps utility.

Purpose: COMP-04 makes the payment amount the brand focal point on order and payment screens. COMP-05 eliminates visual drift across 13 section label instances by defining a single shared utility class.

Output: `@utility caps` defined in globals.css; all 13 inline caps-pattern occurrences across 8 files replaced with `className="caps"`; two grand total elements upgraded to `text-2xl font-black text-primary`.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-component-polish/11-CONTEXT.md
@.planning/phases/11-component-polish/11-RESEARCH.md

<interfaces>
<!-- Key contracts and exact current state for executor. -->

globals.css — current end of file (after the @layer base block):
The file ends around line 200 with @layer base rules. The @utility caps block must be placed AFTER the @theme inline block and @layer base block. Use Tailwind v4 @utility syntax (NOT @layer utilities — that is v3):
```css
@utility caps {
  font-size: theme(--font-size-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: theme(--tracking-wide);
  color: var(--muted-foreground);
}
```

TicketPanel.tsx exact lines to change:
- Line ~144: `<p className="text-[11px] text-muted-foreground uppercase tracking-wide font-medium">Total</p>`
  → `<p className="caps">Total</p>`
- Line ~145: `<p className="text-xl font-bold leading-tight">฿{runningTotal.toFixed(0)}</p>`
  → `<p className="text-2xl font-black text-primary">฿{runningTotal.toFixed(0)}</p>`
- Line ~148-153 (Send to Kitchen Button): currently has `className="h-11 px-5 font-semibold"`. If Plan 01 is done first, this will already be updated to `size="cta"` with cleaned className. If Plan 02 runs first (parallel wave), leave this button as-is — Plan 01 handles it.

TotalsSection.tsx exact lines to change (lines 95-97):
```tsx
// Current:
<div className="flex justify-between border-t pt-3">
  <span className="font-bold text-base">Total</span>
  <span className="font-bold text-base">฿{grandTotal.toLocaleString()}</span>
</div>
// After COMP-04:
<div className="flex justify-between border-t pt-3">
  <span className="caps">Total</span>
  <span className="text-2xl font-black text-primary">฿{grandTotal.toLocaleString()}</span>
</div>
```

All 13 inline caps occurrences — exact replacements:

| File | Line | Current classes | Replace with |
|------|------|----------------|--------------|
| TicketPanel.tsx | ~144 | `text-[11px] text-muted-foreground uppercase tracking-wide font-medium` | `caps` |
| SalesSnapshotTab.tsx | ~53 | `text-xs font-semibold text-muted-foreground uppercase tracking-wide` | `caps` |
| KdsBoard.tsx | ~60 | `text-xs font-semibold uppercase tracking-widest text-muted-foreground` | `caps` (tracking-widest standardizes to tracking-wide via utility) |
| KdsRecallTray.tsx | ~18 | `text-[10px] font-semibold text-muted-foreground/60 uppercase` | `caps` (drop /60 opacity modifier; text-xs is correct for standardization) |
| payment/[tableId]/page.tsx | ~165 | `text-sm font-medium text-muted-foreground uppercase mb-2 tracking-wide` | `caps mb-2` (size regression text-sm→text-xs is intentional per CONTEXT.md) |
| EodSummaryTab.tsx | ~110 | `text-xs font-semibold text-muted-foreground uppercase tracking-wide` | `caps` |
| EodSummaryTab.tsx | ~121 | same | `caps` |
| EodSummaryTab.tsx | ~131 | same | `caps` |
| EodSummaryTab.tsx | ~140 | same | `caps` |
| EightySixTab.tsx | ~28 | `text-xs font-semibold text-muted-foreground uppercase tracking-wide` | `caps` |
| OpenTicketsTab.tsx | ~42 | `text-xs font-semibold text-muted-foreground uppercase tracking-wide` | `caps` |
| OpenTicketsTab.tsx | ~95 | same | `caps` |

Note: The "Total" label in TotalsSection.tsx is NOT in this list above because it currently uses `font-bold text-base` (not the caps pattern). After COMP-04 it becomes `caps`.

IMPORTANT do-not-touch cases:
- `KdsItemRow.tsx:73` — `uppercase font-semibold` on a modifier badge — NOT a section label, leave as-is
- `kds/page.tsx:57` — amber DEMO badge — NOT a section label, leave as-is
- `TicketPanel.tsx:119` line noted in research as `text-[10px] font-bold tracking-widest uppercase text-muted-foreground` — this IS in the list; replace with caps
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define @utility caps in globals.css (COMP-05)</name>
  <files>src/app/globals.css</files>
  <action>
Read `src/app/globals.css` in full, then append the `@utility caps` block at the end of the file (after all existing @layer blocks).

Use Tailwind v4 `@utility` syntax — NOT `@layer utilities { .caps { } }` (that is v3 and produces no output in v4):

```css
/* Section label utility — shared caps pattern for section headings across all screens */
@utility caps {
  font-size: theme(--font-size-xs);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: theme(--tracking-wide);
  color: var(--muted-foreground);
}
```

This utility uses `theme()` references (not `@apply`) — more explicit and readable per the research recommendation. The `var(--muted-foreground)` picks up both light and dark mode values automatically via the token architecture.

Do not modify any other part of globals.css. Do not touch the @theme inline block, :root, or .dark sections.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - globals.css ends with an `@utility caps { ... }` block
    - The block uses `theme(--font-size-xs)`, `font-weight: 600`, `text-transform: uppercase`, `theme(--tracking-wide)`, `var(--muted-foreground)`
    - No other lines in globals.css were changed
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Hero price text + caps audit across all files (COMP-04 + COMP-05)</name>
  <files>
    src/components/order/TicketPanel.tsx,
    src/components/payment/TotalsSection.tsx,
    src/components/manager/SalesSnapshotTab.tsx,
    src/components/manager/EodSummaryTab.tsx,
    src/components/manager/EightySixTab.tsx,
    src/components/manager/OpenTicketsTab.tsx,
    src/components/kds/KdsBoard.tsx,
    src/components/kds/KdsRecallTray.tsx,
    src/app/(app)/payment/[tableId]/page.tsx
  </files>
  <action>
Read each file first, then apply the targeted changes. Work file by file.

**TicketPanel.tsx (two changes):**
- Find the "Total" label paragraph (`text-[11px] text-muted-foreground uppercase tracking-wide font-medium`) → replace className with `"caps"`
- Find the running total paragraph (`text-xl font-bold leading-tight`) → replace with `"text-2xl font-black text-primary leading-tight"`
- Also check line ~119 for the pattern `text-[10px] font-bold tracking-widest uppercase text-muted-foreground` → replace with `"caps"`

**TotalsSection.tsx (two changes, COMP-04):**
- Find the "Total" label span (`font-bold text-base`) → replace with `"caps"`
- Find the grand total amount span (`font-bold text-base` on the ฿ amount) → replace with `"text-2xl font-black text-primary"`
- Grand total should be the amount, not the "Total" label. Both spans are in the same `<div className="flex justify-between border-t pt-3">`.

**SalesSnapshotTab.tsx:**
- Find `text-xs font-semibold text-muted-foreground uppercase tracking-wide` → replace with `"caps"`

**EodSummaryTab.tsx (four occurrences):**
- Find all four occurrences of `text-xs font-semibold text-muted-foreground uppercase tracking-wide` → replace each with `"caps"`

**EightySixTab.tsx:**
- Find `text-xs font-semibold text-muted-foreground uppercase tracking-wide` → replace with `"caps"`

**OpenTicketsTab.tsx (two occurrences):**
- Find both occurrences of `text-xs font-semibold text-muted-foreground uppercase tracking-wide` → replace each with `"caps"`

**KdsBoard.tsx:**
- Find `text-xs font-semibold uppercase tracking-widest text-muted-foreground` → replace with `"caps"` (tracking-widest standardizes to tracking-wide via the utility)

**KdsRecallTray.tsx:**
- Find `text-[10px] font-semibold text-muted-foreground/60 uppercase` → replace with `"caps"` (drop the /60 opacity, standardize text size to xs)

**payment/[tableId]/page.tsx:**
- Find `text-sm font-medium text-muted-foreground uppercase mb-2 tracking-wide` → replace with `"caps mb-2"` (the text-sm→text-xs size reduction is intentional per CONTEXT.md)

DO NOT touch:
- Any amber/colored badge that uses uppercase (e.g., DEMO badge in kds/page.tsx)
- `KdsItemRow.tsx` modifier badge uppercase classes
- Any `uppercase` usage that is NOT a section label
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TicketPanel footer running total renders with `text-2xl font-black text-primary`
    - TotalsSection grand total amount renders with `text-2xl font-black text-primary`
    - "Total" label in TicketPanel and TotalsSection uses `caps`
    - All 13 inline caps-pattern occurrences across 8 files replaced with `className="caps"` (or `"caps mb-2"` where mb-2 was present)
    - TypeScript compilation passes with no errors
  </done>
</task>

</tasks>

<verification>
After both tasks: `cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npm run build 2>&1 | tail -30`

Manual visual checks:
1. `/order/[tableId]` — ticket panel footer total should show as large crimson number
2. `/payment/[tableId]` — payment summary grand total should show as large crimson number
3. Navigate all screens — section labels (Manager, KDS, OrderFlow) should all look visually identical
4. Dark mode — crimson totals should adapt (lighter primary token in dark)
</verification>

<success_criteria>
- COMP-04: TicketPanel running total and TotalsSection grand total both use `text-2xl font-black text-primary`
- COMP-05: `@utility caps` defined in globals.css; all 13 inline occurrences replaced; `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-polish/11-02-SUMMARY.md`
</output>
