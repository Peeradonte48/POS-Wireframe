---
phase: 11-component-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/table-map/TableTile.tsx
  - src/components/kds/KdsTicketCard.tsx
  - src/app/(app)/table-map/page.tsx
  - src/app/(app)/order/[tableId]/page.tsx
  - src/app/(app)/payment/[tableId]/page.tsx
  - src/components/order/TicketPanel.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "All primary action buttons (Open Table, Send to Kitchen, Confirm Payment) visually measure 44px tall"
    - "Hovering or pressing a default-variant button shows a crimson glow shadow that responds to dark mode"
    - "Every tappable button produces a scale-down press animation matching TableTile"
    - "The five table status indicators on the floor plan display as filled colored pill chips, not plain text"
    - "Each of the five status pills (Open/Occupied/Reserved/Check Requested/Cleaning) has a visually distinct background color"
    - "KDS ticket status (New/InProgress/Ready) shows a filled pill badge using the same token approach"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "buttonVariants with cta size, active:scale-[0.97] in base, hover glow in default variant"
      contains: "cta"
    - path: "src/components/table-map/TableTile.tsx"
      provides: "STATUS_CONFIG with bgClass field, Badge render for status row"
      contains: "bgClass"
    - path: "src/components/kds/KdsTicketCard.tsx"
      provides: "KDS stage badge using Badge component with status-bg tokens"
      contains: "Badge"
  key_links:
    - from: "button.tsx default variant"
      to: "hover glow shadow"
      via: "hover:shadow-[...] using var(--color-primary)"
      pattern: "hover:shadow"
    - from: "TableTile STATUS_CONFIG bgClass"
      to: "<Badge> render"
      via: "className={`${bgClass} ${textClass} ...`}"
      pattern: "bgClass"
---

<objective>
Apply the bold brand expression to buttons and status indicators.

Purpose: COMP-01 and COMP-02 together establish the two most visible brand moments on every screen — the primary action button (crimson, 44px, glow-on-hover, press scale) and the filled status pill (distinct color fills visible under dim restaurant lighting).

Output: Updated `button.tsx` with `cta` size variant, updated `TableTile.tsx` and `KdsTicketCard.tsx` with filled pill status badges, and primary CTAs on table map, order, and payment screens upgraded to `size="cta"`.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-component-polish/11-CONTEXT.md
@.planning/phases/11-component-polish/11-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/components/ui/button.tsx (current state):
```typescript
// Base CVA string (line 9) — append active:scale-[0.97] transition-transform here
const buttonVariants = cva(
  "group/button inline-flex shrink-0 items-center justify-center rounded-lg border border-transparent bg-clip-padding text-sm font-medium whitespace-nowrap transition-all outline-none select-none ...",
  {
    variants: {
      variant: {
        // COMP-01: Add hover glow to this string only (not base):
        default: "bg-primary text-primary-foreground [a]:hover:bg-primary/80",
        // ... other variants unchanged
      },
      size: {
        default: "h-8 gap-1.5 px-2.5 ...",
        xs: "h-6 ...",
        sm: "h-7 ...",
        lg: "h-9 gap-1.5 px-2.5 ...",
        icon: "size-8",
        // ... other icon sizes
        // COMP-01: ADD here:
        // cta: "h-11 gap-2 px-6 text-sm font-semibold",
      },
    },
    defaultVariants: { variant: "default", size: "default" },
  }
)
```

From src/components/table-map/TableTile.tsx (current state):
```typescript
// STATUS_CONFIG type — add bgClass: string to each entry
const STATUS_CONFIG: Record<TableStatus, {
  borderClass: string
  textClass: string
  // ADD: bgClass: string
  label: string
  Icon: SolarIcon
}> = {
  Open:           { borderClass: 'border-l-status-open',            textClass: 'text-status-open',            label: 'Open',            Icon: RadioLinear },
  Occupied:       { borderClass: 'border-l-status-occupied',        textClass: 'text-status-occupied',        label: 'Occupied',        Icon: UsersGroupRoundedLinear },
  Reserved:       { borderClass: 'border-l-status-reserved',        textClass: 'text-status-reserved',        label: 'Reserved',        Icon: CalendarDateLinear },
  CheckRequested: { borderClass: 'border-l-status-check-requested', textClass: 'text-status-check-requested', label: 'Check Requested', Icon: WalletLinear },
  Cleaning:       { borderClass: 'border-l-status-cleaning',        textClass: 'text-status-cleaning',        label: 'Cleaning',        Icon: StarLinear },
}

// Status row render (line 48) — currently:
<span className={`text-xs ${textClass} flex items-center gap-1`}>
  <Icon size={12} />
  {label}
</span>
// After COMP-02: replace with <Badge> using bgClass + textClass

// Badge is already imported: import { Badge } from '@/components/ui/badge'
```

From src/components/kds/KdsTicketCard.tsx (current state):
```typescript
// KDS stage is: ticket.stage === 'New' | 'InProgress' | 'Ready'
// Currently no stage badge rendered — stage only drives bumpBlocked logic
// COMP-02: Add a stage badge in the header area using status-bg tokens:
// New       → bg-status-open-bg / text-status-open
// InProgress → bg-status-check-requested-bg / text-status-check-requested
// Ready     → bg-status-reserved-bg / text-status-reserved

// KDS ticket card header (lines 43-45):
<div className="px-3 py-2 bg-muted/30 flex justify-between items-center border-b border-border/40">
  <span className="font-bold text-base">{ticket.tableLabel}</span>
  <span className={`font-mono text-sm tabular-nums ${timerColorClass}`}>{display}</span>
</div>
// After COMP-02: add stage Badge between tableLabel and timer, or below the header row

// Badge component: import { Badge } from '@/components/ui/badge'
// KdsTicket type from kds.store: stage: 'New' | 'InProgress' | 'Ready'
```

Token aliases confirmed in globals.css @theme inline:
- bg-status-open-bg, bg-status-occupied-bg, bg-status-reserved-bg
- bg-status-check-requested-bg, bg-status-cleaning-bg
- text-status-open, text-status-occupied, text-status-reserved
- text-status-check-requested, text-status-cleaning

Primary token: --primary (oklch(0.52 0.26 27) light / oklch(0.63 0.26 27) dark)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend buttonVariants — cta size, press scale, default glow (COMP-01)</name>
  <files>src/components/ui/button.tsx</files>
  <action>
Make three targeted additions to `buttonVariants` in `src/components/ui/button.tsx`:

**1. Base CVA string** — append `active:scale-[0.97] transition-transform` to the base class string (after the existing `transition-all`). Note: `transition-all` is already present; `transition-transform` is redundant but the pattern matches TableTile exactly (`active:scale-[0.97] transition-transform`). Just append both classes.

**2. `size` variants** — add a `cta` key after `lg`:
```
cta: "h-11 gap-2 px-6 text-sm font-semibold",
```
This gives 44px height with horizontal padding and explicit font weight. No changes to existing `xs`, `sm`, `default`, `lg`, or icon sizes.

**3. `default` variant** — add hover glow shadow to the existing string. The glow uses the primary CSS variable so it adapts to dark mode automatically:
```
hover:shadow-[0_0_0_3px_color-mix(in_oklch,var(--color-primary)_25%,transparent)]
```
This produces a 3px ring-like spread glow around the button at 25% primary opacity — visible but not loud. No `box-shadow` at rest (not at rest, hover/active only is met by `hover:` prefix).

Full updated `default` variant string:
```
"bg-primary text-primary-foreground [a]:hover:bg-primary/80 hover:shadow-[0_0_0_3px_color-mix(in_oklch,var(--color-primary)_25%,transparent)]"
```

**Button audit — upgrade primary CTAs to `size="cta"`:**
After saving button.tsx, audit and upgrade the following buttons to `size="cta"`:

- `src/app/(app)/table-map/page.tsx` — "Open Table" submit button inside the open-table sheet (look for the Button that calls the table open action)
- `src/components/order/TicketPanel.tsx` line ~148 — "Send to Kitchen" Button. Currently has `className="h-11 px-5 font-semibold"`. Replace with `size="cta"` and remove the now-redundant `h-11 px-5` from className (keep only any remaining classes that differ, if any).
- `src/app/(app)/payment/[tableId]/page.tsx` — "Confirm Payment" button (the primary payment completion action)

For each upgrade: change `size` prop to `size="cta"`. Remove any inline `h-11`, `px-5`, `px-6` classes that duplicate what `cta` now provides. Secondary utility buttons (void, cancel, back, tab switches) stay at their current size.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `buttonVariants` has `cta: "h-11 gap-2 px-6 text-sm font-semibold"` in size variants
    - Base CVA string contains `active:scale-[0.97] transition-transform`
    - `default` variant string contains `hover:shadow-[...]` with `var(--color-primary)`
    - Open Table, Send to Kitchen, and Confirm Payment buttons use `size="cta"`
    - TypeScript compilation passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Filled status pill badges — TableTile + KdsTicketCard (COMP-02)</name>
  <files>src/components/table-map/TableTile.tsx, src/components/kds/KdsTicketCard.tsx</files>
  <action>
**TableTile.tsx — STATUS_CONFIG bgClass + Badge render:**

1. Extend the `STATUS_CONFIG` type to include `bgClass: string`.

2. Add `bgClass` to every entry using the confirmed @theme inline aliases (short form works because `--color-status-*-bg` aliases exist):
```typescript
Open:           { ..., bgClass: 'bg-status-open-bg' }
Occupied:       { ..., bgClass: 'bg-status-occupied-bg' }
Reserved:       { ..., bgClass: 'bg-status-reserved-bg' }
CheckRequested: { ..., bgClass: 'bg-status-check-requested-bg' }
Cleaning:       { ..., bgClass: 'bg-status-cleaning-bg' }
```

3. Extract `bgClass` from the STATUS_CONFIG destructure on line 35:
```typescript
const { borderClass, textClass, bgClass, label, Icon } = STATUS_CONFIG[table.status]
```

4. Replace the status row `<span>` (line 48) with a `<Badge>` that uses the filled pill style:
```tsx
<Badge className={`${bgClass} ${textClass} flex items-center gap-1 border-0`}>
  <Icon size={12} />
  {label}
</Badge>
```
The `border-0` prevents the default Badge outline border from showing through the filled background. `Badge` is already imported.

**KdsTicketCard.tsx — stage badge in header:**

1. Add `import { Badge } from '@/components/ui/badge'` at the top (check if already imported; add if not).

2. Define a `STAGE_CONFIG` lookup at the top of the component or above it:
```typescript
const KDS_STAGE_CONFIG = {
  New:        { bgClass: 'bg-status-open-bg',             textClass: 'text-status-open' },
  InProgress: { bgClass: 'bg-status-check-requested-bg',  textClass: 'text-status-check-requested' },
  Ready:      { bgClass: 'bg-status-reserved-bg',         textClass: 'text-status-reserved' },
} as const
```
Rationale: New = green (awaiting action), InProgress = amber (active work), Ready = blue (done/waiting collection).

3. In the card header, add the stage badge between the table label and the timer:
```tsx
<div className="px-3 py-2 bg-muted/30 flex justify-between items-center border-b border-border/40">
  <div className="flex items-center gap-2">
    <span className="font-bold text-base">{ticket.tableLabel}</span>
    <Badge className={`${KDS_STAGE_CONFIG[ticket.stage].bgClass} ${KDS_STAGE_CONFIG[ticket.stage].textClass} border-0 text-xs`}>
      {ticket.stage === 'InProgress' ? 'In Progress' : ticket.stage}
    </Badge>
  </div>
  <span className={`font-mono text-sm tabular-nums ${timerColorClass}`}>{display}</span>
</div>
```
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - STATUS_CONFIG has `bgClass` field for all five statuses using short alias form
    - TableTile status row renders as a filled `<Badge>` with background and text color
    - KdsTicketCard header shows stage badge (New/In Progress/Ready) with distinct fills
    - TypeScript compilation passes with no errors
  </done>
</task>

</tasks>

<verification>
After both tasks: `cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npm run build 2>&1 | tail -30`

Manual visual checks (run dev server):
1. Navigate to `/table-map` — all five status states should show as colored pills, not plain text
2. Tap any primary button — should animate to scale-[0.97] on press
3. Hover a primary button in desktop browser — crimson glow ring should appear
4. Navigate to `/kds` — tickets should show New/In Progress/Ready as colored pills in the header
</verification>

<success_criteria>
- COMP-01: `buttonVariants` has `cta` size at `h-11`, base has `active:scale-[0.97] transition-transform`, `default` variant has `hover:shadow-[...]` glow using `var(--color-primary)`
- COMP-02: All five table status states display as filled pills; KDS stage badge added with three distinct colors
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-polish/11-01-SUMMARY.md`
</output>
