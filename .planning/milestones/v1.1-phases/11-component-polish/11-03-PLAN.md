---
phase: 11-component-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - src/components/order/TicketPanel.tsx
  - src/components/order/MenuPanel.tsx
  - src/components/table-map/TableBottomSheet.tsx
  - src/components/manager/EodSummaryTab.tsx
  - src/components/manager/SalesSnapshotTab.tsx
  - src/components/payment/ReceiptScreen.tsx
autonomous: false
requirements:
  - COMP-03

must_haves:
  truths:
    - "Menu item cards on the order screen have a subtle raised shadow — visually lifted from the page background"
    - "The TicketPanel column has a panel-tier shadow — deeper than menu cards, creating visual separation from the menu area"
    - "The TableBottomSheet floating panel has the deepest shadow — clearly floating above the table grid"
    - "Manager EOD summary cards and SalesSnapshot stat cards have a flat card shadow — consistent spatial depth"
    - "Receipt screen detail cards have a panel-tier shadow — elevated container feel matching the checkout moment"
  artifacts:
    - path: "src/components/order/MenuPanel.tsx"
      provides: "menu card elements with style={{ boxShadow: 'var(--shadow-card)' }}"
      contains: "var(--shadow-card)"
    - path: "src/components/order/TicketPanel.tsx"
      provides: "TicketPanel column div with style={{ boxShadow: 'var(--shadow-panel)' }}"
      contains: "var(--shadow-panel)"
    - path: "src/components/table-map/TableBottomSheet.tsx"
      provides: "bottom sheet panel with style={{ boxShadow: 'var(--shadow-floating)' }} replacing shadow-lg"
      contains: "var(--shadow-floating)"
  key_links:
    - from: "globals.css --shadow-card / --shadow-panel / --shadow-floating"
      to: "component style props"
      via: "style={{ boxShadow: 'var(--shadow-*)' }}"
      pattern: "var\\(--shadow-"
    - from: "TableBottomSheet"
      to: "floating tier"
      via: "replace shadow-lg class with inline style prop"
      pattern: "shadow-floating"
---

<objective>
Apply the three-tier elevation system to cards, panels, and floating surfaces across all screens.

Purpose: COMP-03 creates clear spatial hierarchy — menu cards feel raised above the background, the ticket panel feels deeper than the menu, and bottom sheets feel like they're floating above the grid. Depends on Plan 02 because TicketPanel.tsx was already edited there (hero total + caps) and this plan adds the elevation style prop to the same file.

Output: Six component files updated with `style={{ boxShadow: 'var(--shadow-*)' }}` inline props matching the three elevation tiers. `shadow-lg` Tailwind class removed from TableBottomSheet in favor of the floating token.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-component-polish/11-CONTEXT.md
@.planning/phases/11-component-polish/11-RESEARCH.md
@.planning/phases/11-component-polish/11-02-SUMMARY.md

<interfaces>
<!-- Shadow token values confirmed in globals.css. Elevation assignment per research doc. -->

Elevation tier assignment:
- --shadow-card    (flat tier)    → menu cards, manager EOD cards, SalesSnapshot stat cards
- --shadow-panel   (raised tier)  → TicketPanel column, ReceiptScreen detail cards
- --shadow-floating (floating tier) → TableBottomSheet bottom sheet panel

Shadow tokens (confirmed values in globals.css):
Light mode:
  --shadow-card:     0 1px 3px 0 oklch(0 0 0 / 0.08), 0 1px 2px -1px oklch(0 0 0 / 0.06)
  --shadow-panel:    0 4px 6px -1px oklch(0 0 0 / 0.08), 0 2px 4px -2px oklch(0 0 0 / 0.05)
  --shadow-floating: 0 10px 15px -3px oklch(0 0 0 / 0.10), 0 4px 6px -4px oklch(0 0 0 / 0.07)
Dark mode:
  --shadow-card:     0 0 0 1px oklch(1 0 0 / 0.06)
  --shadow-panel:    0 0 0 1px oklch(1 0 0 / 0.08), 0 4px 12px oklch(0 0 0 / 0.40)
  --shadow-floating: 0 0 0 1px oklch(1 0 0 / 0.10), 0 8px 24px oklch(0 0 0 / 0.50)

Constraint: Shadow tokens MUST be applied via `style={{ boxShadow: 'var(--shadow-card)' }}` (inline style prop) — NOT as a Tailwind class. Multi-value CSS strings are incompatible with Tailwind v4 @theme inline. This is a confirmed Phase 10 decision.

Exact elements to target:

MenuPanel.tsx — menu item card button (line ~70):
Current: `className="group bg-card rounded-xl overflow-hidden border text-left transition-all duration-200"`
Add: `style={{ boxShadow: 'var(--shadow-card)' }}`

MenuPanel.tsx — menu item skeleton card (line ~18):
Current: `className="bg-card rounded-xl overflow-hidden border border-border"`
Add: `style={{ boxShadow: 'var(--shadow-card)' }}`

order/[tableId]/page.tsx — TicketPanel column wrapper div (line ~102):
Current: `className="w-56 md:w-64 lg:w-72 xl:w-80 border-l border-border flex flex-col bg-card shrink-0"`
Note: This div is in `src/app/(app)/order/[tableId]/page.tsx` NOT in TicketPanel.tsx itself.
Add: `style={{ boxShadow: 'var(--shadow-panel)' }}`

TableBottomSheet.tsx — the sliding panel div (line ~70):
Current className contains: `shadow-lg transition-transform duration-300 ease-out max-h-[80vh] overflow-y-auto`
Change: Remove `shadow-lg` from className, add `style={{ boxShadow: 'var(--shadow-floating)' }}`

EodSummaryTab.tsx — all four card divs (lines ~109, ~120, ~130, ~139):
Current: `className="rounded-lg border bg-card p-4 space-y-2"` (and `space-y-3` on last)
Add: `style={{ boxShadow: 'var(--shadow-card)' }}` to each

SalesSnapshotTab.tsx — stat card and summary card (lines ~52, ~76):
Current: `className="rounded-lg border bg-card p-4 space-y-2"` and `className="rounded-lg border bg-card p-3 space-y-0.5"`
Add: `style={{ boxShadow: 'var(--shadow-card)' }}` to each

ReceiptScreen.tsx — detail card divs (lines ~40, ~75):
Current: `className="rounded-xl border bg-card p-6 w-full max-w-sm space-y-3"` and similar
Add: `style={{ boxShadow: 'var(--shadow-panel)' }}` to each
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply elevation to order screen — MenuPanel + TicketPanel column (COMP-03)</name>
  <files>
    src/components/order/MenuPanel.tsx,
    src/app/(app)/order/[tableId]/page.tsx
  </files>
  <action>
**MenuPanel.tsx — add shadow-card to both card elements:**

Read `src/components/order/MenuPanel.tsx` in full first.

1. Find the main menu item button card (around line 70):
   `className="group bg-card rounded-xl overflow-hidden border text-left transition-all duration-200"`
   → Add `style={{ boxShadow: 'var(--shadow-card)' }}` as a prop on this element (alongside className).

2. Find the skeleton loading card (around line 18):
   `className="bg-card rounded-xl overflow-hidden border border-border"`
   → Add `style={{ boxShadow: 'var(--shadow-card)' }}` as a prop.

**order/[tableId]/page.tsx — add shadow-panel to TicketPanel column wrapper:**

Read `src/app/(app)/order/[tableId]/page.tsx` in full first.

Find the TicketPanel column wrapper div (line ~102):
`className="w-56 md:w-64 lg:w-72 xl:w-80 border-l border-border flex flex-col bg-card shrink-0"`
→ Add `style={{ boxShadow: 'var(--shadow-panel)' }}` as a prop on this div.

Do not modify anything else in page.tsx.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - MenuPanel menu item card button has `style={{ boxShadow: 'var(--shadow-card)' }}`
    - MenuPanel skeleton card has `style={{ boxShadow: 'var(--shadow-card)' }}`
    - TicketPanel column wrapper in page.tsx has `style={{ boxShadow: 'var(--shadow-panel)' }}`
    - TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply elevation to floating sheet, manager cards, receipt cards (COMP-03)</name>
  <files>
    src/components/table-map/TableBottomSheet.tsx,
    src/components/manager/EodSummaryTab.tsx,
    src/components/manager/SalesSnapshotTab.tsx,
    src/components/payment/ReceiptScreen.tsx
  </files>
  <action>
Read each file before editing.

**TableBottomSheet.tsx — floating tier:**

Find the sliding panel div (~line 70) whose className contains `shadow-lg transition-transform duration-300 ease-out max-h-[80vh] overflow-y-auto`.

1. Remove `shadow-lg` from the className string.
2. Add `style={{ boxShadow: 'var(--shadow-floating)' }}` as a prop on the same element.

The element might look like:
```tsx
<div
  className="... shadow-lg transition-transform duration-300 ease-out max-h-[80vh] overflow-y-auto"
```
After:
```tsx
<div
  style={{ boxShadow: 'var(--shadow-floating)' }}
  className="... transition-transform duration-300 ease-out max-h-[80vh] overflow-y-auto"
```

**EodSummaryTab.tsx — flat card tier (4 cards):**

Find all four card divs with `className="rounded-lg border bg-card p-4 space-y-2"` (and `space-y-3` for the last one). Add `style={{ boxShadow: 'var(--shadow-card)' }}` to each.

**SalesSnapshotTab.tsx — flat card tier (2 cards):**

Find the two card divs:
- `className="rounded-lg border bg-card p-4 space-y-2"` (~line 52)
- `className="rounded-lg border bg-card p-3 space-y-0.5"` (~line 76)
Add `style={{ boxShadow: 'var(--shadow-card)' }}` to each.

**ReceiptScreen.tsx — raised panel tier (2 cards):**

Find the two detail card divs with `className="rounded-xl border bg-card ..."` (~lines 40 and 75). Add `style={{ boxShadow: 'var(--shadow-panel)' }}` to each.
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - TableBottomSheet has `style={{ boxShadow: 'var(--shadow-floating)' }}` and `shadow-lg` removed from className
    - EodSummaryTab all 4 cards have `style={{ boxShadow: 'var(--shadow-card)' }}`
    - SalesSnapshotTab both cards have `style={{ boxShadow: 'var(--shadow-card)' }}`
    - ReceiptScreen both cards have `style={{ boxShadow: 'var(--shadow-panel)' }}`
    - `npm run build` exits 0 — TypeScript + Tailwind compilation green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 11 visual sign-off</name>
  <what-built>
    Complete Phase 11 Component Polish across all five COMP requirements:
    - COMP-01: Button cta size (44px), press scale, crimson hover glow
    - COMP-02: Filled status pills on table map and KDS
    - COMP-03: Three-tier elevation on cards, panels, and sheets
    - COMP-04: Hero price totals in crimson on order and payment screens
    - COMP-05: Caps utility applied across all 13 section label occurrences
  </what-built>
  <how-to-verify>
    Start dev server: `npm run dev`

    1. TABLE MAP (/table-map):
       - All 5 table status states show as filled colored pills (not plain text spans)
       - Open = green bg, Occupied = red bg, Reserved = blue bg, Check Requested = amber bg, Cleaning = muted bg
       - Tapping an available table — "Open Table" button should be tall (44px) and show press scale animation

    2. ORDER SCREEN (/order/[tableId]):
       - Menu item cards have a subtle shadow (lifted from white background)
       - Ticket panel on the right has a deeper shadow than the menu area
       - Section labels in ticket panel ("Total" label, any heading) are small caps
       - Running total at ticket footer shows as large crimson number
       - "Send to Kitchen" button is tall (44px)

    3. PAYMENT SCREEN (/payment/[tableId]):
       - Grand total in payment summary is large, bold, and crimson
       - "Confirm Payment" button is tall (44px)
       - "Items" section label is small caps

    4. KDS (/kds):
       - Each ticket card header shows a colored stage badge (New = green, In Progress = amber, Ready = blue)
       - Section headings on KDS board are small caps

    5. MANAGER (/manager):
       - EOD summary section labels are small caps
       - EOD summary cards have a subtle card shadow
       - SalesSnapshot stat cards have a subtle card shadow

    6. DARK MODE (toggle via theme button if available):
       - Crimson totals use lighter primary token (brighter, not washed out)
       - Hover glow on buttons is visible (adapts to dark primary)
       - Status pill backgrounds adapt to dark mode colors
       - Table bottom sheet floating shadow is visible (border glow in dark mode)

    7. BOTTOM SHEET (tap occupied table):
       - TableBottomSheet sliding panel visually floats above the grid
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found</resume-signal>
  <action>Present the verification checklist and wait for user response.</action>
  <verify><automated>echo "checkpoint — human verification required"</automated></verify>
  <done>User types "approved" confirming all five COMP requirements are visually correct across all screens in light and dark mode.</done>
</task>

</tasks>

<verification>
Final build gate: `cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npm run build && npm run lint 2>&1 | tail -40`
</verification>

<success_criteria>
- COMP-03: All six elevation targets have `style={{ boxShadow: 'var(--shadow-*)' }}` with correct tier assignment
- TableBottomSheet `shadow-lg` removed and replaced with floating token
- `npm run build && npm run lint` exits 0
- Visual verification checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/11-component-polish/11-03-SUMMARY.md`
</output>
