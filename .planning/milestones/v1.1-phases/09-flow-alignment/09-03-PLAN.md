---
phase: 09-flow-alignment
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/payment/ReceiptScreen.tsx
autonomous: false
requirements:
  - FLOW-05

must_haves:
  truths:
    - "ReceiptScreen shows a loyalty section below the payment details card and above the action buttons"
    - "The loyalty section displays 'Gold Member' with '1,240 pts' mock data"
    - "A QR placeholder box is visible with label 'Customer scans to earn points'"
    - "An annotation text '[Smart loyalty QR — unique per bill, baked with spend + branch]' appears below the QR box"
    - "The loyalty section is visually distinct from the payment confirmation card above it"
  artifacts:
    - path: "src/components/payment/ReceiptScreen.tsx"
      provides: "Loyalty section block in receipt view"
      contains: "Gold Member"
  key_links:
    - from: "ReceiptScreen.tsx loyalty section"
      to: "action buttons (Reprint / Back to Floor)"
      via: "JSX insertion point — loyalty block rendered between details card and action buttons"
      pattern: "loyalty"
---

<objective>
Add a CRM Type 2 (smart loyalty) section to ReceiptScreen, showing mock member tier data and a QR placeholder for point collection, making the post-payment loyalty story visible in the wireframe.

Purpose: FLOW-05 from the v1.1 user flow spec — stakeholders need to see that the receipt state bridges the payment completion and the loyalty earning moment.
Output: One modified component file. No new dependencies, no new stores.
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-flow-alignment/09-CONTEXT.md
@.planning/phases/09-flow-alignment/09-RESEARCH.md

<interfaces>
<!-- Key layout structure of ReceiptScreen. Executor inserts the loyalty block at the correct point. -->

From src/components/payment/ReceiptScreen.tsx (confirmed layout order):
```
1. Header / title
2. Payment details card  <- insert loyalty section AFTER this
3. [NEW] Loyalty section
4. Action buttons: Reprint + Back to Floor  <- loyalty section goes BEFORE these
```

Annotation pattern (used throughout payment screen):
```tsx
<p className="text-xs text-muted-foreground">[annotation text]</p>
```

Loyalty section JSX to insert (from CONTEXT.md — exact content locked by user decision):
```tsx
<div className="rounded-xl border bg-card p-4 w-full max-w-sm space-y-3">
  {/* Member info row */}
  <div className="flex items-center justify-between">
    <span className="text-sm font-medium">Gold Member</span>
    <span className="text-sm text-muted-foreground">1,240 pts</span>
  </div>

  {/* QR placeholder box */}
  <div className="border-2 border-dashed rounded-lg h-24 flex items-center justify-center">
    <span className="text-xs text-muted-foreground text-center">
      Customer scans to earn points
    </span>
  </div>

  {/* Annotation */}
  <p className="text-xs text-muted-foreground text-center">
    [Smart loyalty QR — unique per bill, baked with spend + branch]
  </p>
</div>
```

Note: Visual styling of the loyalty card (border, background, icon treatment) is Claude's discretion per CONTEXT.md. The content (Gold Member, 1,240 pts, annotation text) is locked. A subtle gold accent for the tier indicator is encouraged — e.g. `text-amber-500` on the member label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FLOW-05 — Add loyalty section to ReceiptScreen</name>
  <files>src/components/payment/ReceiptScreen.tsx</files>
  <action>
Locate the JSX in ReceiptScreen.tsx and find the insertion point: after the payment details card and before the action buttons (Reprint / Back to Floor).

Insert the following loyalty section block at that position:

```tsx
{/* Loyalty Section — CRM Type 2 (Smart Loyalty) */}
<div className="rounded-xl border bg-card p-4 w-full max-w-sm space-y-3">
  {/* Member tier and point balance */}
  <div className="flex items-center justify-between">
    <span className="text-sm font-medium text-amber-500">Gold Member</span>
    <span className="text-sm text-muted-foreground">1,240 pts</span>
  </div>

  {/* QR placeholder — customer scans to earn points */}
  <div className="border-2 border-dashed rounded-lg h-24 flex items-center justify-center bg-muted/30">
    <span className="text-xs text-muted-foreground text-center px-2">
      Customer scans to earn points
    </span>
  </div>

  {/* Wireframe annotation */}
  <p className="text-xs text-muted-foreground text-center">
    [Smart loyalty QR — unique per bill, baked with spend + branch]
  </p>
</div>
```

Rules:
- Insert AFTER the payment details card JSX block, BEFORE the action buttons section
- Do not modify any existing JSX outside this insertion
- Do not import any new library or component — this is plain JSX with Tailwind only
- The `text-amber-500` gold accent on the member label is the discretion-level styling choice
- The `bg-muted/30` on the QR box is discretion-level — it gives the dashed box a subtle fill distinct from the card background
- Static mock data only — no props, no state, no dynamic values
  </action>
  <verify>
    <automated>cd "/Users/peeradonte/Desktop/Tech Basecamp/A RAMEN/POS-wireframe" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. ReceiptScreen renders the loyalty section with Gold Member tier, 1,240 pts, QR placeholder box, and annotation text. The block appears between the payment details card and the Reprint/Back to Floor buttons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all five Phase 9 flows in the browser</name>
  <files>none</files>
  <action>Human verification step — no code changes. Run dev server and walk through all five FLOW requirements as described in how-to-verify.</action>
  <what-built>
All five Phase 9 flow alignment changes are implemented:
- FLOW-01: Open Table guest count starts empty, confirm button disabled until entry
- FLOW-02: "Served at HH:MM" appears in Occupied bottom sheet after tapping Served
- FLOW-03: Scan Coupon QR button + camera viewfinder sheet (auto-close 1.5s, mock RAMEN50 coupon)
- FLOW-04: QrPanel shows "(after ฿50 discount)" when coupon applied and QR PromptPay selected
- FLOW-05: ReceiptScreen has Gold Member loyalty section with QR placeholder
  </what-built>
  <how-to-verify>
Run the dev server: `npm run dev` then open http://localhost:3000

**FLOW-01:** Login as any role → tap any Open table → Open Table modal appears with EMPTY guest count → confirm button is greyed out → type "2" → confirm button activates

**FLOW-02:** Open a table (enter guest count) → add items and send order → go back to floor map → tap the occupied table → tap "Served" → bottom sheet shows "Served at HH:MM" time

**FLOW-03 + FLOW-04:** Open a table → add items → send to kitchen → tap "Charge" → on the payment screen, the coupon area shows a "Scan Coupon QR" button (no text input) → tap it → camera viewfinder sheet slides up with pulsing border → after 1.5s it auto-closes → "RAMEN50 −฿50" appears in totals → switch payment method to QR PromptPay → QR panel shows the net amount with "(after ฿50 discount)" label

**FLOW-05:** Complete a payment → receipt screen appears → loyalty section is visible below the payment details card showing "Gold Member | 1,240 pts", a dashed QR placeholder box, and the annotation text
  </how-to-verify>
  <verify>Visual browser walkthrough of all five flows as described above</verify>
  <done>All five FLOW interactions visible and working correctly in the browser. No regressions on other screens.</done>
  <resume-signal>Type "approved" if all five flows look correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build && npm run lint` passes
- All five FLOW requirements verified visually in the browser
- No regressions in existing screens (floor map, order flow, KDS, manager)
</verification>

<success_criteria>
- FLOW-05: Loyalty section is visible on ReceiptScreen with correct mock data and annotation
- All five FLOW requirements (FLOW-01 through FLOW-05) pass the manual walkthrough
- Zero TypeScript errors — build green
</success_criteria>

<output>
After completion, create `.planning/phases/09-flow-alignment/09-03-SUMMARY.md`
</output>
