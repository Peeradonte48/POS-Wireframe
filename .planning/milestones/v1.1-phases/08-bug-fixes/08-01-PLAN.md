---
phase: 08-bug-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/orders/page.tsx
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "Tapping Orders in the sidebar never produces a 404"
    - "A staff member arriving at /orders lands on the table map with a helpful hint"
  artifacts:
    - path: "src/app/(app)/orders/page.tsx"
      provides: "Stub redirect page for /orders route"
      contains: "redirect"
  key_links:
    - from: "src/app/(app)/orders/page.tsx"
      to: "/table-map"
      via: "next/navigation redirect"
      pattern: "redirect.*table-map"
---

<objective>
Create a stub page at /orders that immediately redirects staff to /table-map with a toast hint explaining that orders are opened from the table map.

Purpose: The sidebar links /orders but no route exists, producing a 404. Staff need a graceful destination rather than a broken page.
Output: src/app/(app)/orders/page.tsx — a server component that calls redirect('/table-map').
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/app-shell/AppSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /orders stub redirect page</name>
  <files>src/app/(app)/orders/page.tsx</files>
  <action>
Create a new Server Component at src/app/(app)/orders/page.tsx.

The page must:
1. Import `redirect` from `'next/navigation'`
2. Call `redirect('/table-map')` as the only statement in the default export function

This is a server-side redirect — no client code, no hooks, no JSX needed. The redirect happens before any render, so staff never see a flash of content.

Note: The 'orders' NavSlug in ROLE_NAV_ACCESS must NOT be changed — it controls sidebar visibility. Only the missing route is being fixed here.

```typescript
import { redirect } from 'next/navigation'

export default function OrdersPage() {
  redirect('/table-map')
}
```

The file is ~5 lines. No imports beyond redirect are needed.
  </action>
  <verify>
    Run: `npx tsc --noEmit` — no TypeScript errors.
    Manual: Log in as Waiter, open shift, tap Orders in sidebar. Verify the browser navigates to /table-map without a 404 error page.
  </verify>
  <done>
    Navigating to /orders (via sidebar tap or direct URL) redirects to /table-map. No 404 is shown. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
Manual smoke test (BUG-01):
1. Log in as Waiter, open shift
2. Tap "Orders" in the sidebar
3. Expected: Browser lands on /table-map, no 404 page
4. Also verify: Type /orders directly in URL bar → same redirect behavior
</verification>

<success_criteria>
Staff can tap the Orders link in the sidebar and reach the table map without a 404 error page appearing.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes/08-01-SUMMARY.md`
</output>
