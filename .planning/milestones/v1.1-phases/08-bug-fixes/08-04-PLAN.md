---
phase: 08-bug-fixes
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/role-permissions.ts
  - src/components/order/TicketPanel.tsx
  - src/components/order/TicketLineItem.tsx
autonomous: true
requirements:
  - BUG-04

must_haves:
  truths:
    - "A Waiter who sends an item to kitchen sees no void button on that sent item"
    - "A Manager who sends an item to kitchen sees the void button on that sent item"
    - "void-post-send is a recognized ActionKey with Manager-only permission"
  artifacts:
    - path: "src/lib/role-permissions.ts"
      provides: "ActionKey type and ACTION_PERMISSIONS with void-post-send entry"
      contains: "void-post-send"
    - path: "src/components/order/TicketLineItem.tsx"
      provides: "TicketLineItem with separate canVoidSent prop for sent-branch void button"
      contains: "canVoidSent"
    - path: "src/components/order/TicketPanel.tsx"
      provides: "TicketPanel passing canVoidSent={canDoAction(role, 'void-post-send')}"
      contains: "void-post-send"
  key_links:
    - from: "src/components/order/TicketPanel.tsx"
      to: "src/components/order/TicketLineItem.tsx"
      via: "canVoidSent prop"
      pattern: "canVoidSent.*canDoAction.*void-post-send"
    - from: "src/components/order/TicketLineItem.tsx"
      to: "void button on sent branch"
      via: "canVoidSent gate"
      pattern: "canVoidSent"
---

<objective>
Add 'void-post-send' to the ActionKey type and ACTION_PERMISSIONS (Manager only), then wire the correct permission check through TicketPanel to TicketLineItem for the sent-item void button.

Purpose: The action key is absent, so the void button on sent items uses the pre-send permission (too permissive). After this fix, only Manager sees the void button on sent items.
Output: role-permissions.ts (type + map updated), TicketLineItem.tsx (canVoidSent prop added), TicketPanel.tsx (passes canVoidSent with void-post-send check).
</objective>

<execution_context>
@/Users/peeradonte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peeradonte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/role-permissions.ts
@src/components/order/TicketPanel.tsx
@src/components/order/TicketLineItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add void-post-send to ActionKey and ACTION_PERMISSIONS</name>
  <files>src/lib/role-permissions.ts</files>
  <action>
Read src/lib/role-permissions.ts in full, then make two additions:

**Addition 1 — ActionKey type:**
Find the `export type ActionKey` union type. Add `'void-post-send'` as a new member. Place it after `'void-pre-send'` for logical grouping:

```typescript
export type ActionKey =
  | 'open-table'
  | 'mark-reserved'
  | 'request-check'
  | 'send-to-kitchen'
  | 'void-pre-send'
  | 'void-post-send'   // ADD — void after order sent to kitchen
  | 'confirm-payment'
  | 'eighty-six-toggle'
  | 'close-shift'
  | 'kds-bump'
  | 'mark-served'
```

**Addition 2 — ACTION_PERMISSIONS map:**
Find the `ACTION_PERMISSIONS` object. Add the new entry after the `'void-pre-send'` entry:

```typescript
'void-post-send': ['Manager'],   // Only Manager can void after send
```

The existing `ManagerPinModal` flow in TicketPanel already provides authorization escalation for the void-after-send action. The UI gate here is the permission check that controls whether the button is visible at all.

Do not change any other entries in ACTION_PERMISSIONS.
  </action>
  <verify>
    Run: `npx tsc --noEmit` — no TypeScript errors (Record<ActionKey, Role[]> will now enforce the new key is present).
  </verify>
  <done>
    ActionKey type includes 'void-post-send'. ACTION_PERMISSIONS has entry 'void-post-send': ['Manager']. TypeScript Record constraint is satisfied (no missing key errors).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire canVoidSent prop through TicketPanel to TicketLineItem</name>
  <files>src/components/order/TicketLineItem.tsx, src/components/order/TicketPanel.tsx</files>
  <action>
Read both files in full before editing.

**TicketLineItem.tsx changes:**

1. Find the props interface (likely `TicketLineItemProps` or similar). Add a new optional prop:
   ```typescript
   canVoidSent?: boolean
   ```
   Add it near the existing `canRemove` prop for clarity.

2. Find the JSX for the sent-item branch (the branch that renders when an item has been sent to kitchen — check for a condition like `item.sent`, `isSent`, or similar). In that branch, find the void/trash button element.

3. The current button likely uses `canRemove` or is always visible. Change it to use `canVoidSent`:
   - If the button is always rendered: add a condition `{canVoidSent && <button ...>}`
   - If the button already checks `canRemove`: change that specific instance to check `canVoidSent`
   - The unsent-branch trash button must keep using `canRemove` — only change the sent-branch void button

**TicketPanel.tsx changes:**

1. Find where `TicketLineItem` is rendered inside the component (there may be a `.map()` over items or separate renders for sent/unsent items).

2. Find the prop currently passed for sent-item void gating. Based on research, `TicketPanel` passes `canRemove={canDoAction(role, 'void-pre-send')}` for all items including sent ones.

3. Add a separate prop for the sent-item void gate. Pass it alongside (not replacing) the existing canRemove prop:
   ```typescript
   canVoidSent={canDoAction(role, 'void-post-send')}
   ```

4. If TicketPanel renders TicketLineItem in only one place (with a prop differentiating sent vs unsent), pass canVoidSent there. If it renders in two separate branches (one for sent, one for unsent), only add canVoidSent to the sent-item render.

Watch-out: Do NOT remove or rename the existing `canRemove` prop — it controls the unsent-item trash button and must remain. This is an additive change: new prop + new usage for the sent branch only.
  </action>
  <verify>
    Run: `npx tsc --noEmit` — no TypeScript errors.
    Manual (BUG-04a): Log in as Waiter, open a table, add a menu item, tap "Send to Kitchen". On the ticket, find the sent item. The void/trash button on the sent item must be HIDDEN or not rendered.
    Manual (BUG-04b): Log in as Manager, open a table, add a menu item, tap "Send to Kitchen". The void/trash button on the sent item must be VISIBLE.
    Manual (regression): Log in as Waiter, add an item (do not send). The unsent item's trash/remove button must still be visible (canRemove behavior unchanged).
  </verify>
  <done>
    Waiter sees no void button on sent items. Manager sees void button on sent items. Unsent item remove buttons work for all previously permitted roles (Waiter, Cashier, Manager). TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
Manual smoke tests (BUG-04):
1. Waiter → add item → send to kitchen → sent item void button is hidden
2. Manager → add item → send to kitchen → sent item void button is visible
3. Waiter → add item (unsent) → remove button is still visible (regression)
4. Type-check: `npx tsc --noEmit` passes
</verification>

<success_criteria>
The void-after-send action is visible only to Manager. Non-Manager roles see no void UI on sent items. The permission key void-post-send exists in the type system and is enforced at the UI level.
</success_criteria>

<output>
After completion, create `.planning/phases/08-bug-fixes/08-04-SUMMARY.md`
</output>
